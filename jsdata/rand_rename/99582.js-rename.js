"use strict";const assert=require("assert");const request=require("../../test-helpers/context").request;describe("req.header",()=>{it("should return the request header object",()=>{const e=request();assert.deepStrictEqual(e.header,e.req.headers)});it("should set the request header object",()=>{const e=request();e.header={"X-Custom-Headerfield":"Its one header, with headerfields"};assert.deepStrictEqual(e.header,e.req.headers)})});