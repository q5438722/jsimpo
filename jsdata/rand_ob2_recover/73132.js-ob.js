const {addComment} = require(_0xbe739a(426)), {addLabels} = require('./add-labels'), {closeOpen} = require('./close-open'), {labeler} = require(_0xbe739a(427));
module['exports'] = {
    'addComment': addComment,
    'addLabels': addLabels,
    'closeOpen': closeOpen,
    'labeler': labeler
};