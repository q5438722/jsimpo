'use strict';
let Array = function updateBestTileAtCurrentLevel() {
  const resetCoverage = _0x34a1;
  babelHelpers[resetCoverage(299)](this, updateBestTileAtCurrentLevel);
};
let List = function(other) {
  function cellProps() {
    const now = updateDevicesAfterDelay;
    return babelHelpers[now(299)](this, cellProps), rpm_traffic[now(302)](this, arguments);
  }
  const updateDevicesAfterDelay = _0x34a1;
  babelHelpers[updateDevicesAfterDelay(300)](cellProps, other);
  var rpm_traffic = babelHelpers[updateDevicesAfterDelay(301)](cellProps);
  return cellProps;
}(Array);

