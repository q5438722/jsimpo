'use strict';
for (let i = 0, count = array; i < count["length"]; i++) {
  const [_windowTask, ...body] = count[i];
  {
    const header = 1;
    console["log"](body);
    console["log"](header);
  }
}
;
