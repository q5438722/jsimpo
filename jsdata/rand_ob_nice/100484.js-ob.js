'use strict';
const tOutResp = ["663095rHZwwG", "398262fOMFic", "253345SWGgQl", "298391OCsHCY", "3054xmclEE", "83vLctKz", "webL10n", "_lang", "_ready", "wtild", "setLanguage", "NxmNS", "XcpQM", "getLanguage", "get", "translate", "3UYbAOX", "74999AkHRRu", "116702JJqzfk"];
const prefixed = m;
(function(data, val) {
  const toMonths = m;
  for (; !![];) {
    try {
      const nodeval = -parseInt(toMonths(262)) * -parseInt(toMonths(263)) + parseInt(toMonths(264)) + parseInt(toMonths(265)) + -parseInt(toMonths(266)) + -parseInt(toMonths(267)) + parseInt(toMonths(268)) + -parseInt(toMonths(269)) * parseInt(toMonths(270));
      if (nodeval === val) {
        break;
      } else {
        data["push"](data["shift"]());
      }
    } catch (_0x43cb11) {
      data["push"](data["shift"]());
    }
  }
})(tOutResp, 3 * -151562 + -5 * 36448 + -1035022 * -1);
function m(b, i) {
  return m = function(value, i) {
    value = value - (-81 * 49 + 8706 + -4475 * 1);
    let d = tOutResp[value];
    return d;
  }, m(b, i);
}
import "../external/webL10n/l10n.js";
import{
  fixupLangCode as fixupLangCode,
  getL10nFallback as getL10nFallback
}from "./l10n_utils.js";
const lastviewmatrix = document[prefixed(271)];
class ArborKnight {
  constructor(player) {
    const ChromosomeInfo = prefixed;
    const _cmdInterceptors = {
      "wtild" : function(saveNotifs, notifications) {
        return saveNotifs(notifications);
      },
      "NxmNS" : function(saveNotifs, notifications) {
        return saveNotifs(notifications);
      }
    };
    this[ChromosomeInfo(272)] = player;
    this[ChromosomeInfo(273)] = new Promise((data, canCreateDiscussions) => {
      const isListLikeIterable = ChromosomeInfo;
      const rpm_traffic = {
        "XcpQM" : function(args, player) {
          const updateDevicesAfterDelay = m;
          return _cmdInterceptors[updateDevicesAfterDelay(274)](args, player);
        }
      };
      lastviewmatrix[isListLikeIterable(275)](_cmdInterceptors[isListLikeIterable(276)](fixupLangCode, player), () => {
        const now = isListLikeIterable;
        rpm_traffic[now(277)](data, lastviewmatrix);
      });
    });
  }
  async[prefixed(278)]() {
    const now = prefixed;
    const rpm_traffic = await this[now(273)];
    return rpm_traffic[now(278)]();
  }
  async["getDirection"]() {
    const gotoNewOfflinePage = prefixed;
    const _0x398c6e = await this[gotoNewOfflinePage(273)];
    return _0x398c6e["getDirection"]();
  }
  async[prefixed(279)](data, offset = null, valuesWritten = getL10nFallback(data, offset)) {
    const now = prefixed;
    const techniqueParameterBuffer = await this[now(273)];
    return techniqueParameterBuffer[now(279)](data, offset, valuesWritten);
  }
  async["translate"](data) {
    const now = prefixed;
    const rpm_traffic = await this[now(273)];
    return rpm_traffic[now(280)](data);
  }
}
export{
  ArborKnight as GenericL10n
};

