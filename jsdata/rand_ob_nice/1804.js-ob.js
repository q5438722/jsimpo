'use strict';
const data_rr = ["1wIYVAl", "202LJDZDB", "4297jFfiKY", "597396wkRgZS", "transition", "emit", "ztUkG", "1307836dwkHwY", "230471aVwvic", "1057416JnHsgp", "1379269ShXlAl", "457846zYSJER"];
function done(err, value) {
  return done = function(time, value) {
    time = time - (-5 * -227 + 4547 * 1 + 204 * -26);
    let _done = data_rr[time];
    return _done;
  }, done(err, value);
}
(function(data, val) {
  const toMonths = done;
  for (; !![];) {
    try {
      const nodeval = -parseInt(toMonths(378)) + -parseInt(toMonths(379)) + parseInt(toMonths(380)) + parseInt(toMonths(381)) + -parseInt(toMonths(382)) * parseInt(toMonths(383)) + parseInt(toMonths(384)) * parseInt(toMonths(385)) + -parseInt(toMonths(386));
      if (nodeval === val) {
        break;
      } else {
        data["push"](data["shift"]());
      }
    } catch (_0x28d3dc) {
      data["push"](data["shift"]());
    }
  }
})(data_rr, -506333 + -2441 * 569 + 2606392);
export default function d3_scale_linearRebind(scale, linear) {
  const rel2Mstr = done;
  const el = {
    "ztUkG" : "setTransition"
  };
  const same = this;
  if (!same["params"]["cssMode"]) {
    same["$wrapperEl"][rel2Mstr(387)](scale);
  }
  same[rel2Mstr(388)](el[rel2Mstr(389)], scale, linear);
}
;
