'use strict';
const data_rr = ["UXxKQ", "kRZpd", "203857HERnfK", "1ClIMDT", "10073kaEIOC", "22bccVSO", "173717arXZiZ", "173359Hycosm", "1ZCFwUL", "249299qRjIEp", "4AAghfC", "31484EeOQlS", "719YooVOq", "1789OJCfqp", "create"];
function done(err, value) {
  return done = function(time, value) {
    time = time - (1 * 4182 + -1783 + -2158);
    let _done = data_rr[time];
    return _done;
  }, done(err, value);
}
(function(data, val) {
  const toMonths = done;
  for (; !![];) {
    try {
      const nodeval = parseInt(toMonths(241)) * -parseInt(toMonths(242)) + parseInt(toMonths(243)) * -parseInt(toMonths(244)) + -parseInt(toMonths(245)) + -parseInt(toMonths(246)) + parseInt(toMonths(247)) * -parseInt(toMonths(248)) + parseInt(toMonths(249)) * -parseInt(toMonths(250)) + -parseInt(toMonths(251)) * -parseInt(toMonths(252));
      if (nodeval === val) {
        break;
      } else {
        data["push"](data["shift"]());
      }
    } catch (_0x1ba557) {
      data["push"](data["shift"]());
    }
  }
})(data_rr, -718 * 19 + -35175 + 187334);
import{
  looseEqual as looseEqual
}from "uni-shared";
function value(fields, k, v) {
  fields[k] = v;
}
function fn(response, t) {
  const _element_ready = done;
  const app = {
    "VScCv" : function(require, load, callback) {
      return require(load, callback);
    }
  };
  let ast;
  let i;
  let data;
  let trigger;
  for (i in response) {
    data = response[i];
    trigger = t[i];
    if (!app["VScCv"](looseEqual, trigger, data)) {
      value(ast || (ast = Object[_element_ready(253)](null)), i, data);
    }
  }
  return ast;
}
function diff(val, config, key) {
  const getYearsDiff = done;
  const tests = {
    "UXxKQ" : function(require, load, callback, id) {
      return require(load, callback, id);
    },
    "kRZpd" : function(require, load, callback) {
      return require(load, callback);
    }
  };
  let k;
  let item;
  let seed;
  for (k in val) {
    item = val[k];
    seed = config[k];
    if (!seed) {
      tests[getYearsDiff(254)](value, key, k, item);
      continue;
    }
    const testName = tests[getYearsDiff(255)](fn, item, seed);
    if (testName) {
      tests[getYearsDiff(254)](value, key, k, testName);
    }
  }
  return key;
}
export{
  diff as diff
};

