'use strict';
const eventHandlersByType = ["23KhuPlp", "13463NRBlBn", "__DC_STAT_UUID", "rnnvE", "CssJv", "now", "floor", "random", "setStorage", "deviceId", "safeArea", "safeAreaInsets", "left", "windowWidth", "right", "FhPJK", "windowHeight", "bottom", "devtools", "ios", "hIJbk", "FvUsr", "indexOf", "platform", "RNmza", "mHFaf", "1MyWhFi", "302581JGfdRn", "217903NoUAgL", "1etIGnW", "1749626EpiKJr", "4CTDDkI", "220445fotQeQ", "1461562neMwat", "667462BPgjrs"];
const eventHandlers = test;
(function(data, val) {
  const toMonths = test;
  for (; !![];) {
    try {
      const nodeval = parseInt(toMonths(264)) * parseInt(toMonths(265)) + parseInt(toMonths(266)) + -parseInt(toMonths(267)) * -parseInt(toMonths(268)) + parseInt(toMonths(269)) * -parseInt(toMonths(270)) + -parseInt(toMonths(271)) + parseInt(toMonths(272)) + -parseInt(toMonths(273)) * -parseInt(toMonths(274));
      if (nodeval === val) {
        break;
      } else {
        data["push"](data["shift"]());
      }
    } catch (_0x33fce0) {
      data["push"](data["shift"]());
    }
  }
})(eventHandlersByType, -110356 + -475 * -1839 + 140710);
function test(type, n) {
  return test = function(type, i) {
    type = type - (-32 * -53 + -1 * 1839 + 11 * 37);
    let command = eventHandlersByType[type];
    return command;
  }, test(type, n);
}
import{
  getStorageSync as getStorageSync
}from "./storage";
const value = eventHandlers(275);
let msg;
function status(message) {
  const statusSet = eventHandlers;
  const view = {
    "rnnvE" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "CssJv" : function(formatters, customFormatters) {
      return formatters + customFormatters;
    }
  };
  msg = msg || view[statusSet(276)](getStorageSync, value);
  if (!msg) {
    msg = view[statusSet(277)](Date[statusSet(278)](), "") + Math[statusSet(279)](Math[statusSet(280)]() * (-5078580 + -1630723 * 12 + 17323628 * 2));
    __GLOBAL__[statusSet(281)]({
      "key" : value,
      "data" : msg
    });
  }
  message[statusSet(282)] = msg;
}
function event(context) {
  const trim = eventHandlers;
  const calcResult = {
    "FhPJK" : function(connectNumber, concurency) {
      return connectNumber - concurency;
    }
  };
  if (context[trim(283)]) {
    const result = context[trim(283)];
    context[trim(284)] = {
      "top" : result["top"],
      "left" : result[trim(285)],
      "right" : calcResult["FhPJK"](context[trim(286)], result[trim(287)]),
      "bottom" : calcResult[trim(288)](context[trim(289)], result[trim(290)])
    };
  }
}
function process(data) {
  const getKey = eventHandlers;
  const node = {
    "hIJbk" : getKey(291),
    "aEnIg" : "android",
    "FvUsr" : getKey(292)
  };
  let id = data["platform"] ? data["platform"]["toLowerCase"]() : node[getKey(293)];
  if (!~[node["aEnIg"], node[getKey(294)]][getKey(295)](id)) {
    id = node["hIJbk"];
  }
  data[getKey(296)] = id;
}
export default{
  "returnValue" : function(value) {
    const forEach = eventHandlers;
    const paintHandler = {
      "RNmza" : function(saveNotifs, notifications) {
        return saveNotifs(notifications);
      },
      "mHFaf" : function(saveNotifs, notifications) {
        return saveNotifs(notifications);
      }
    };
    paintHandler[forEach(297)](status, value);
    paintHandler[forEach(298)](event, value);
    paintHandler[forEach(298)](process, value);
  }
};

