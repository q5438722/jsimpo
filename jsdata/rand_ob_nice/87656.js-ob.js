'use strict';
const tOutResp = ["jEfAM", "ozZJJ", "contains", "observe", "canvas", "removedNodes", "glscf", "drNQr", "forEach", "currentDevicePixelRatio", "resize", "size", "OqWdI", "set", "delete", "SpQzS", "bxEDe", "clientWidth", "contentRect", "OvwZn", "Fwdlf", "sUZsJ", "disconnect", "gDRLL", "XSyeM", "ctx", "pewJm", "offsetX", "getContext", "aOsub", "hfmHa", "initial", "MmDNK", "removeAttribute", "setAttribute", "keys", "$proxies", "devicePixelRatio", "getMaximumSize", "isAttached", "MrpUO", "isConnected", 
"382240npgpEh", "8VYQsTs", "61839Ahwshu", "8jITTRa", "49446StYkFp", "138918BDBTlf", "2yDiQnX", "19986hySkgb", "3RpJXoK", "1owUYBo", "477172hHVBEx", "1063823hREVkf", "mousedown", "mousemove", "mouseup", "mouseenter", "mouseout", "height", "block", "style", "getAttribute", "dVyAT", "display", "width", "boxSizing", "LWaSG", "ffyYF", "dPuzE", "KoACT", "FMrRS", "UyoTp", "addEventListener", "removeEventListener", "rQkmh"];
const normalizeEvent = m;
(function(data, val) {
  const toMonths = m;
  for (; !![];) {
    try {
      const nodeval = -parseInt(toMonths(143)) + -parseInt(toMonths(144)) * -parseInt(toMonths(145)) + -parseInt(toMonths(146)) * -parseInt(toMonths(147)) + parseInt(toMonths(148)) * parseInt(toMonths(149)) + parseInt(toMonths(150)) * parseInt(toMonths(151)) + -parseInt(toMonths(152)) * -parseInt(toMonths(153)) + -parseInt(toMonths(154));
      if (nodeval === val) {
        break;
      } else {
        data["push"](data["shift"]());
      }
    } catch (_0x159ef8) {
      data["push"](data["shift"]());
    }
  }
})(tOutResp, 248896 * 2 + 124115 * -1 + 23 * -4978);
import _0x3a96c6 from "./platform.base";
import{
  _getParentNode as _getParentNode,
  getRelativePosition as getRelativePosition,
  supportsEventListenerOptions as supportsEventListenerOptions,
  readUsedSize as readUsedSize,
  getMaximumSize as getMaximumSize
}from "../helpers/helpers.dom";
import{
  throttled as throttled
}from "../helpers/helpers.extras";
import{
  isNullOrUndef as isNullOrUndef
}from "../helpers/helpers.core";
const prop = "$chartjs";
const item = {
  "touchstart" : normalizeEvent(155),
  "touchmove" : normalizeEvent(156),
  "touchend" : normalizeEvent(157),
  "pointerenter" : normalizeEvent(158),
  "pointerdown" : normalizeEvent(155),
  "pointermove" : normalizeEvent(156),
  "pointerup" : "mouseup",
  "pointerleave" : "mouseout",
  "pointerout" : normalizeEvent(159)
};
const val = (value) => {
  return value === null || value === "";
};
function load(value, offset) {
  const parseInt = normalizeEvent;
  const buf = {
    "dVyAT" : parseInt(160),
    "ffyYF" : "width",
    "atNKy" : parseInt(161),
    "LWaSG" : "border-box",
    "hTkNU" : function(require, load, callback) {
      return require(load, callback);
    },
    "dPuzE" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "KoACT" : function(name, initialValue) {
      return name === initialValue;
    },
    "FMrRS" : function(_num1, _num2) {
      return _num1 / _num2;
    },
    "UyoTp" : function(require, load, callback) {
      return require(load, callback);
    }
  };
  const data = value[parseInt(162)];
  const n = value[parseInt(163)](buf[parseInt(164)]);
  const progress = value["getAttribute"](buf["ffyYF"]);
  value[prop] = {
    "initial" : {
      "height" : n,
      "width" : progress,
      "style" : {
        "display" : data[parseInt(165)],
        "height" : data[parseInt(160)],
        "width" : data[parseInt(166)]
      }
    }
  };
  data[parseInt(165)] = data[parseInt(165)] || buf["atNKy"];
  data[parseInt(167)] = data["boxSizing"] || buf[parseInt(168)];
  if (val(progress)) {
    const v = buf["hTkNU"](readUsedSize, value, buf[parseInt(169)]);
    if (v !== undefined) {
      value["width"] = v;
    }
  }
  if (buf[parseInt(170)](val, n)) {
    if (buf[parseInt(171)](value[parseInt(162)]["height"], "")) {
      value["height"] = buf[parseInt(172)](value["width"], offset || -3 * -800 + 1 * -2659 + 261);
    } else {
      const innerTrans = buf[parseInt(173)](readUsedSize, value, buf[parseInt(164)]);
      if (innerTrans !== undefined) {
        value[parseInt(160)] = innerTrans;
      }
    }
  }
  return value;
}
const root = supportsEventListenerOptions ? {
  "passive" : !![]
} : ![];
function message(p, val, cb) {
  const el = normalizeEvent;
  p[el(174)](val, cb, root);
}
function text(params, id, elem) {
  const ESC = normalizeEvent;
  params["canvas"][ESC(175)](id, elem, root);
}
function error(props, idx) {
  const afterPageOne = normalizeEvent;
  const boxModelProps = {
    "rQkmh" : function(require, load, callback) {
      return require(load, callback);
    },
    "jEfAM" : function(value, joiner) {
      return value !== joiner;
    }
  };
  const GET_USER_PROFILE_SUCCESS = item[props["type"]] || props["type"];
  const {
    x : x,
    y : y
  } = boxModelProps[afterPageOne(176)](getRelativePosition, props, idx);
  return {
    "type" : GET_USER_PROFILE_SUCCESS,
    "chart" : idx,
    "native" : props,
    "x" : boxModelProps[afterPageOne(177)](x, undefined) ? x : null,
    "y" : y !== undefined ? y : null
  };
}
function record(options, metadata, key) {
  const now = normalizeEvent;
  const constraint = {
    "ozZJJ" : function(name, initialValue) {
      return name === initialValue;
    },
    "gSzkg" : function(saveNotifs) {
      return saveNotifs();
    }
  };
  const value = options["canvas"];
  const rpm_traffic = new MutationObserver((unscreenedStyles) => {
    const now = m;
    for (const keys of unscreenedStyles) {
      for (const key of keys["addedNodes"]) {
        if (constraint[now(178)](key, value) || key[now(179)](value)) {
          return constraint["gSzkg"](key);
        }
      }
    }
  });
  return rpm_traffic[now(180)](document, {
    "childList" : !![],
    "subtree" : !![]
  }), rpm_traffic;
}
function ready(fn, val, data) {
  const observe = normalizeEvent;
  const command_codes = {
    "glscf" : function(saveNotifs) {
      return saveNotifs();
    }
  };
  const line = fn[observe(181)];
  const _updateDeep2 = new MutationObserver((outputs) => {
    const trim = observe;
    for (const output of outputs) {
      for (const NO_STACK_TRACE of output[trim(182)]) {
        if (NO_STACK_TRACE === line || NO_STACK_TRACE[trim(179)](line)) {
          return command_codes[trim(183)](data);
        }
      }
    }
  });
  return _updateDeep2["observe"](document, {
    "childList" : !![],
    "subtree" : !![]
  }), _updateDeep2;
}
const tests = new Map;
function m(b, i) {
  return m = function(value, i) {
    value = value - (12 * 470 + 1 * -3611 + -1886);
    let d = tOutResp[value];
    return d;
  }, m(b, i);
}
let _diff = -6454 + -68 + 2174 * 3;
function handler() {
  const ensureDeveloperIsRegistered = normalizeEvent;
  const inBtn = {
    "dMLsb" : function(value, joiner) {
      return value !== joiner;
    },
    "drNQr" : function(name, initialValue) {
      return name === initialValue;
    }
  };
  const d = window["devicePixelRatio"];
  if (inBtn[ensureDeveloperIsRegistered(184)](d, _diff)) {
    return;
  }
  _diff = d;
  tests[ensureDeveloperIsRegistered(185)]((getLinkInfo, types) => {
    const type = ensureDeveloperIsRegistered;
    if (inBtn["dMLsb"](types[type(186)], d)) {
      getLinkInfo();
    }
  });
}
function style(value, extra) {
  const getType = normalizeEvent;
  const typeEditors = {
    "OqWdI" : getType(187)
  };
  if (!tests[getType(188)]) {
    window[getType(174)](typeEditors[getType(189)], handler);
  }
  tests[getType(190)](value, extra);
}
function event(value) {
  const trim = normalizeEvent;
  const data = {
    "SpQzS" : trim(187)
  };
  tests[trim(191)](value);
  if (!tests[trim(188)]) {
    window["removeEventListener"](data[trim(192)], handler);
  }
}
function callback(data, ifLast, next) {
  const getKey = normalizeEvent;
  const ossService = {
    "Fwdlf" : function(require, load, callback) {
      return require(load, callback);
    },
    "bxEDe" : function(progressOld, progressNew) {
      return progressOld < progressNew;
    },
    "OvwZn" : function(name, initialValue) {
      return name === initialValue;
    },
    "froUe" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "sUZsJ" : function(require, load, callback) {
      return require(load, callback);
    }
  };
  const axes = data[getKey(181)];
  const transform = axes && ossService["froUe"](_getParentNode, axes);
  if (!transform) {
    return;
  }
  const isPublic = ossService["sUZsJ"](throttled, (mmCoreSplitViewBlock, isPublic) => {
    const convertProfileToArray = getKey;
    const arrayBuffer = transform["clientWidth"];
    ossService["Fwdlf"](next, mmCoreSplitViewBlock, isPublic);
    if (ossService[convertProfileToArray(193)](arrayBuffer, transform[convertProfileToArray(194)])) {
      next();
    }
  }, window);
  const obj = new ResizeObserver((canCreateDiscussions) => {
    const parseInt = getKey;
    const colData = canCreateDiscussions[-6096 + 4109 + 1987];
    const cdv = colData[parseInt(195)][parseInt(166)];
    const n = colData[parseInt(195)][parseInt(160)];
    if (ossService[parseInt(196)](cdv, 4236 + -6143 + 1907) && n === -9836 + -83 * 4 + 1271 * 8) {
      return;
    }
    ossService[parseInt(197)](isPublic, cdv, n);
  });
  return obj[getKey(180)](transform), ossService[getKey(198)](style, data, isPublic), obj;
}
function resize(endDate, duration, options) {
  const parseInt = normalizeEvent;
  const com = {
    "gDRLL" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    }
  };
  if (options) {
    options[parseInt(199)]();
  }
  if (duration === parseInt(187)) {
    com[parseInt(200)](event, endDate);
  }
}
function next(action, value, cb) {
  const activeSlideHasBulletByOffset = normalizeEvent;
  const console = {
    "gaBUL" : function(value, joiner) {
      return value !== joiner;
    },
    "pewJm" : function(require, load, callback) {
      return require(load, callback);
    },
    "XSyeM" : function(require, load, callback, id) {
      return require(load, callback, id);
    }
  };
  const ext = action[activeSlideHasBulletByOffset(181)];
  const result = console[activeSlideHasBulletByOffset(201)](throttled, (data) => {
    const reduceAll = activeSlideHasBulletByOffset;
    if (console["gaBUL"](action[reduceAll(202)], null)) {
      cb(console[reduceAll(203)](error, data, action));
    }
  }, action, (canCreateDiscussions) => {
    const now = activeSlideHasBulletByOffset;
    const rpm_traffic = canCreateDiscussions[-3458 + 4878 + 355 * -4];
    return [rpm_traffic, rpm_traffic[now(204)], rpm_traffic["offsetY"]];
  });
  return console[activeSlideHasBulletByOffset(201)](message, ext, value, result), result;
}
export default class DomPlatform extends _0x3a96c6 {
  ["acquireContext"](column, value) {
    const now = normalizeEvent;
    const rpm_traffic = {
      "aOsub" : function(name, initialValue) {
        return name === initialValue;
      },
      "hfmHa" : function(require, load, callback) {
        return require(load, callback);
      }
    };
    const showActiveColumns = column && column["getContext"] && column[now(205)]("2d");
    if (showActiveColumns && rpm_traffic[now(206)](showActiveColumns["canvas"], column)) {
      return rpm_traffic[now(207)](load, column, value), showActiveColumns;
    }
    return null;
  }
  ["releaseContext"](currentItems) {
    const parseInt = normalizeEvent;
    const atomToChunk = {
      "eClZH" : parseInt(160),
      "MmDNK" : parseInt(166)
    };
    const obj = currentItems[parseInt(181)];
    if (!obj[prop]) {
      return ![];
    }
    const animationConfigs = obj[prop][parseInt(208)];
    [atomToChunk["eClZH"], atomToChunk[parseInt(209)]]["forEach"]((event) => {
      const getKey = parseInt;
      const deep = animationConfigs[event];
      if (isNullOrUndef(deep)) {
        obj[getKey(210)](event);
      } else {
        obj[getKey(211)](event, deep);
      }
    });
    const descriptor = animationConfigs[parseInt(162)] || {};
    return Object[parseInt(212)](descriptor)[parseInt(185)]((key) => {
      const getKey = parseInt;
      obj[getKey(162)][key] = descriptor[key];
    }), obj[parseInt(166)] = obj["width"], delete obj[prop], !![];
  }
  [normalizeEvent(174)](context, i, state) {
    const getResponsesAnalysisDataPrefixCacheKey = normalizeEvent;
    this[getResponsesAnalysisDataPrefixCacheKey(175)](context, i);
    const ret = context[getResponsesAnalysisDataPrefixCacheKey(213)] || (context[getResponsesAnalysisDataPrefixCacheKey(213)] = {});
    const stack = {
      "attach" : record,
      "detach" : ready,
      "resize" : callback
    };
    const render = stack[i] || next;
    ret[i] = render(context, i, state);
  }
  [normalizeEvent(175)](initialViewModelData, key) {
    const cleanVMName = normalizeEvent;
    const taint = initialViewModelData[cleanVMName(213)] || (initialViewModelData[cleanVMName(213)] = {});
    const lastviewmatrix = taint[key];
    if (!lastviewmatrix) {
      return;
    }
    const dictionary = {
      "attach" : resize,
      "detach" : resize,
      "resize" : resize
    };
    const test_vector = dictionary[key] || text;
    test_vector(initialViewModelData, key, lastviewmatrix);
    taint[key] = undefined;
  }
  ["getDevicePixelRatio"]() {
    const d3_vendorSymbol = normalizeEvent;
    return window[d3_vendorSymbol(214)];
  }
  [normalizeEvent(215)](mmCoreSplitViewBlock, mmaPushNotificationsComponent, mmaFrontpagePriority, isBgroundImg) {
    return getMaximumSize(mmCoreSplitViewBlock, mmaPushNotificationsComponent, mmaFrontpagePriority, isBgroundImg);
  }
  [normalizeEvent(216)](leftFence) {
    const now = normalizeEvent;
    const rpm_traffic = {
      "MrpUO" : function(saveNotifs, notifications) {
        return saveNotifs(notifications);
      }
    };
    const _oAtomIgnoreFiles = rpm_traffic[now(217)](_getParentNode, leftFence);
    return !!(_oAtomIgnoreFiles && _oAtomIgnoreFiles[now(218)]);
  }
}
;
