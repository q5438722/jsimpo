'use strict';
const URL_AVATAR_ICONS = ["readFile", "CQSHY", "ZOpcs", "Vdovm", "writeFile", "\n      const App = ({ Component, pageProps }) => <Component {...pageProps} />\n      App.getInitialProps = async (ctx) => {\n        let pageProps = {}\n        if (ctx.Component.getInitialProps) {\n          pageProps = await ctx.Component.getInitialProps(ctx.ctx)\n        }\n        return { pageProps }\n      }\n      export default App\n    ", "plPMA", "appGip", "CnHBy", "remove", "gip identifiers", "dev mode", "production mode", 
"ggume", "KPtKs", "BkSJK", "320783lmGooj", "427ZNwzEw", "911SxREOU", "3457cmMwaq", "398gdVJoY", "810942leuwAC", "2JNRNYd", "341053QWEmuA", "1328274HpImJB", "2074255Vjfwnb", "1YZWSfo", "setTimeout", "pages/index.js", "should not have gip or appGip in NEXT_DATA for page without getInitialProps", "should have gip in NEXT_DATA for page with getInitialProps", "should only have appGip in NEXT_DATA for page without getInitialProps and _app with getInitialProps", "UtRmq", "NhDIe", "oqekc", "build faild, exit code: ", 
"Bxotq", "load", "parse", "qIKLO", "#__NEXT_DATA__", "text", "iDKOO", "JhwrZ", "gip", "toBe", "XiFHz"];
const maybeBlock = m;
(function(data, val) {
  const toMonths = m;
  for (; !![];) {
    try {
      const nodeval = parseInt(toMonths(443)) + parseInt(toMonths(444)) * -parseInt(toMonths(445)) + -parseInt(toMonths(446)) * -parseInt(toMonths(447)) + parseInt(toMonths(448)) + parseInt(toMonths(449)) * -parseInt(toMonths(450)) + parseInt(toMonths(451)) + parseInt(toMonths(452)) * -parseInt(toMonths(453));
      if (nodeval === val) {
        break;
      } else {
        data["push"](data["shift"]());
      }
    } catch (_0x30538a) {
      data["push"](data["shift"]());
    }
  }
})(URL_AVATAR_ICONS, -212448 * 2 + 1371499 + -256076);
import _0x511c44 from "cheerio";
function m(b, i) {
  return m = function(value, i) {
    value = value - (-8876 + -71 * 102 + 16561);
    let d = URL_AVATAR_ICONS[value];
    return d;
  }, m(b, i);
}
import _0x31afcb from "fs-extra";
import{
  findPort as findPort,
  killApp as killApp,
  launchApp as launchApp,
  nextBuild as nextBuild,
  nextStart as nextStart,
  renderViaHTTP as renderViaHTTP
}from "next-test-utils";
import{
  join as join
}from "path";
jest[maybeBlock(454)]((3265 + 9246 + -11511) * (-1 * -4604 + -3632 + -912) * (1988 + 3 * -799 + 411));
const URL_ICON_FOLDER = join(__dirname, "..");
const URL_ICON_INFO = join(URL_ICON_FOLDER, "pages/_app.js");
const URL_ICON_LIST = join(URL_ICON_FOLDER, maybeBlock(455));
let syncFlushOpt;
let callback;
let postDateGmt;
const transformer = (text) => {
  const chainContainsUtil = maybeBlock;
  const zlib = {
    "Bxotq" : function(saveNotifs) {
      return saveNotifs();
    },
    "UtRmq" : function(require, load, callback) {
      return require(load, callback);
    },
    "NhDIe" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "oqekc" : function(value, joiner) {
      return value !== joiner;
    },
    "qIKLO" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "RsYIm" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "CQSHY" : "utf8",
    "ZOpcs" : function(saveNotifs) {
      return saveNotifs();
    },
    "Vdovm" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "plPMA" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "AuDfW" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "iDKOO" : function(require, load, callback) {
      return require(load, callback);
    },
    "JhwrZ" : chainContainsUtil(456),
    "XiFHz" : chainContainsUtil(457),
    "CnHBy" : chainContainsUtil(458)
  };
  const result = async() => {
    const buildSpan = chainContainsUtil;
    if (text) {
      callback = await zlib["Bxotq"](findPort);
      syncFlushOpt = await zlib[buildSpan(459)](launchApp, URL_ICON_FOLDER, callback);
    } else {
      const {
        code : result
      } = await zlib[buildSpan(460)](nextBuild, URL_ICON_FOLDER);
      if (zlib[buildSpan(461)](result, 15 * -381 + 105 * 79 + -430 * 6)) {
        throw new Error(buildSpan(462) + result);
      }
      callback = await zlib[buildSpan(463)](findPort);
      syncFlushOpt = await nextStart(URL_ICON_FOLDER, callback);
    }
    const artistTrack = await renderViaHTTP(callback, "/");
    await zlib[buildSpan(460)](killApp, syncFlushOpt);
    const result = _0x511c44[buildSpan(464)](artistTrack);
    return JSON[buildSpan(465)](zlib[buildSpan(466)](result, buildSpan(467))[buildSpan(468)]());
  };
  zlib[chainContainsUtil(469)](it, zlib[chainContainsUtil(470)], async() => {
    const Boolean = chainContainsUtil;
    const boolIcons = await zlib[Boolean(463)](result);
    zlib[Boolean(466)](expect, boolIcons[Boolean(471)])["toBe"](undefined);
    zlib["RsYIm"](expect, boolIcons["appGip"])[Boolean(472)](undefined);
  });
  zlib["iDKOO"](it, zlib[chainContainsUtil(473)], async() => {
    const Boolean = chainContainsUtil;
    postDateGmt = await _0x31afcb[Boolean(474)](URL_ICON_LIST, zlib[Boolean(475)]);
    await _0x31afcb["writeFile"](URL_ICON_LIST, "\n      const Page = () => 'hi'\n      Page.getInitialProps = () => ({ hello: 'world' })\n      export default Page\n    ");
    const boolIcons = await zlib[Boolean(476)](result);
    zlib[Boolean(477)](expect, boolIcons[Boolean(471)])[Boolean(472)](!![]);
  });
  it("should have gip and appGip in NEXT_DATA for page with getInitialProps and _app with getInitialProps", async() => {
    const Boolean = chainContainsUtil;
    await _0x31afcb[Boolean(478)](URL_ICON_INFO, Boolean(479));
    const boolIcons = await zlib["ZOpcs"](result);
    zlib["plPMA"](expect, boolIcons["gip"])[Boolean(472)](!![]);
    zlib[Boolean(480)](expect, boolIcons[Boolean(481)])["toBe"](!![]);
  });
  zlib[chainContainsUtil(469)](it, zlib[chainContainsUtil(482)], async() => {
    const filter = chainContainsUtil;
    await _0x31afcb[filter(478)](URL_ICON_LIST, postDateGmt);
    const _0x35136b = await zlib[filter(476)](result);
    await _0x31afcb[filter(483)](URL_ICON_INFO);
    zlib["AuDfW"](expect, _0x35136b["gip"])[filter(472)](undefined);
    zlib["AuDfW"](expect, _0x35136b["appGip"])[filter(472)](!![]);
  });
};
describe(maybeBlock(484), () => {
  const now = maybeBlock;
  const rpm_traffic = {
    "BCwmk" : function(saveNotifs, notifications) {
      return saveNotifs(notifications);
    },
    "NbrIo" : function(saveNotifs) {
      return saveNotifs();
    },
    "KPtKs" : function(require, load, callback) {
      return require(load, callback);
    },
    "ggume" : now(485),
    "BkSJK" : now(486)
  };
  rpm_traffic["KPtKs"](describe, rpm_traffic[now(487)], () => {
    rpm_traffic["BCwmk"](transformer, !![]);
  });
  rpm_traffic[now(488)](describe, rpm_traffic[now(489)], () => {
    rpm_traffic["NbrIo"](transformer);
  });
});

