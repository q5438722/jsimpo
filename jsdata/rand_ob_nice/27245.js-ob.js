'use strict';
const URL_AVATAR_ICONS = ["1421481vMTJYZ", "1SJnpwK", "277866mJIJTg", "974783yeNFPo", "61944xrBrgl", "993LmlWVd", "805LBkBHa", "#ff5252", "1149345yQTTxL", "549314aPKBLl", "1cvQcze"];
const readFile = m;
function m(b, i) {
  return m = function(value, i) {
    value = value - (1243 + -18 * 30 + 4 * -80);
    let d = URL_AVATAR_ICONS[value];
    return d;
  }, m(b, i);
}
(function(data, val) {
  const toMonths = m;
  for (; !![];) {
    try {
      const nodeval = parseInt(toMonths(383)) + -parseInt(toMonths(384)) + parseInt(toMonths(385)) * -parseInt(toMonths(386)) + parseInt(toMonths(387)) * -parseInt(toMonths(388)) + parseInt(toMonths(389)) + parseInt(toMonths(390)) + parseInt(toMonths(391)) * parseInt(toMonths(392));
      if (nodeval === val) {
        break;
      } else {
        data["push"](data["shift"]());
      }
    } catch (_0x4dabb5) {
      data["push"](data["shift"]());
    }
  }
})(URL_AVATAR_ICONS, 913491 + 1 * 609907 + -786622);
import{
  createMuiTheme as createMuiTheme,
  createTheme as createTheme,
  adaptV4Theme as adaptV4Theme
}from "@material-ui/core/styles";
const URL_ICON_DOWNLOAD = createMuiTheme();
const URL_ICON_DRIVE_DOCUMENT = createTheme();
const URL_ICON_DRIVE_SPREADSHEET = createMuiTheme(adaptV4Theme({
  "palette" : {
    "primary" : {
      "main" : readFile(393)
    }
  }
}));
const URL_ICON_GRID = createTheme(adaptV4Theme({
  "palette" : {
    "primary" : {
      "main" : readFile(393)
    }
  }
}));

