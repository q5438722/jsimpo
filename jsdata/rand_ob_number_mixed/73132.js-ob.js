const {addComment} = require('./add-comment'), {addLabels} = require('./add-labels'), {closeOpen} = require('./close-open'), {labeler} = require('./labeler');
module['exports'] = {
    'addComment': addComment,
    'addLabels': addLabels,
    'closeOpen': closeOpen,
    'labeler': labeler
};
