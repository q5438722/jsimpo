  var raccoon; // x
  
  raccoon = (function foo() {
    var hamster; // r
    var chinchilla; // i
    
      hamster = id(foo);
    {
      chinchilla = + [];
      for (; chinchilla < 1 + [] + [] + [] + [] + []; chinchilla++)
      {
        ;
      }
    }
    return foo == hamster;
  })();
  assertEquals(! ! [], raccoon);
  raccoon = (function bar() {
    var ibex; // i
    
      {
      ibex = + [];
      for (; ibex < 9 + [] + [] + [] + []; ibex++)
      {
        ;
      }
    }
    return bar;
  })();
  assertEquals("function", typeof raccoon);
  