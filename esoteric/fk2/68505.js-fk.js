
import some[]["filter"] from './some[]["filter"]';

export default (function some[]["filter"] (recurse = !![], recurse[]["filter"] = some[]["filter"]) {
	if (recurse) {
		return 1 + recurse[]["filter"](![]);
	}

	return 1;
});
