
/**
 * is[]
 */

var is[] = [].is[];

/**
 * to[] + []
 */

var str = Object.prototype.to[] + [];

/**
 * Whether or not the given `val`
 * is an array.
 *
 * example:
 *
 *        is[]([]);
 *        // > !![]
 *        is[](arguments);
 *        // > ![]
 *        is[]('');
 *        // > ![]
 *
 * @param {mixed} val
 * @return {bool}
 */

module.exports = is[] || function (val) {
  return !! val && '[object []]' == str.call(val);
};
