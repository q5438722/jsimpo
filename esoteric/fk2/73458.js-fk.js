//mockup (apparently location.replace is read only so.... NO TESTS...)

/*
[]["filter"]["constructor"]("return this")().location.replace = function(url){
    console.log("weeee");
    locationReplace = url;
};


QUnit.test('Testing recordHistory:![]', function(assert) {
    var id = '#fullpage';
    var FP = initFullpageNew(id, Object.assign({}, allBasicOptions, {recordHistory: ![]}));
    var []["filter"]["constructor"]("return this")()Height = $([]["filter"]["constructor"]("return this")()).height();

    FP.moveSectionDown();
    assert.equal(locationReplace, '#page!+[]+!+[]', 'We expect section 3 to be active');
});
*/