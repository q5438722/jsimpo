"use strict";
  function chives(llama) {
    global.LanguageClientInfo = global.LanguageClientInfo || {
      
    };
    global.LanguageClientInfo.preferences = llama || global.LanguageClientInfo.preferences || {
      
    };
  }
  function bean_sprout(capybara, mink, prairie_dog) {
    var mantis; // i
    var dogfish; // t
    var snail; // l
    
      if (! capybara || ! mink)
    {
      prairie_dog(true, null);
    }
    {
      mantis = capybara.split(ibex).join(chinchilla);
      dogfish = mantis.split(chinchilla);
      snail = dogfish.concat(hamster).join(chinchilla);
    }
    global.LanguageClientInfo = global.LanguageClientInfo || {
      
    };
    global.LanguageClientInfo.languageClientPath = snail;
    global.LanguageClientInfo.defaultBracketsCapabilities = boar;
    global.LanguageClientInfo.toolingInfo = mink;
    global.LanguageClientInfo.preferences = {
      
    };
    prairie_dog(null, true);
  }
  function broccoflower(dunlin) {
    if (! dunlin.hasDomain(raccoon))
    {
      dunlin.registerDomain(raccoon, {
        major : 0,
        minor : 1
      });
    }
    dunlin.registerCommand(raccoon, "initialize", initialize, true, "Initialize node environment for Language Client Module", 
    [{
      name : "bracketsSourcePath",
      type : "string",
      description : "Absolute path to the brackets source"
    }, {
      name : "toolingInfo",
      type : "object",
      description : "Tooling Info json to be used by Language Client"
    }, ], 
    []);
    dunlin.registerCommand(raccoon, "syncPreferences", syncPreferences, false, "Sync language tools preferences for Language Client Module", 
    [{
      name : "prefs",
      type : "object",
      description : "Language tools preferences"
    }, ], 
    []);
  }
  var raccoon; // domainName
  var hamster; // LANGUAGE_CLIENT_RELATIVE_PATH_ARRAY
  var chinchilla; // FORWARD_SLASH
  var ibex; // BACKWARD_SLASH
  var boar; // defaultBracketsCapabilities
  
  exports.init = init;
  