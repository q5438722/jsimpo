"use strict";
  function chives(capybara, mink, prairie_dog) {
    var mantis; // o
    var dogfish; // f
    var snail; // a
    var dunlin; // s
    
      {
      dogfish = {
        
      };
      snail = null;
      dunlin = null;
    }
    void 0 !== prairie_dog && (snail = "" + prairie_dog);
    void 0 !== mink.key && (snail = "" + mink.key);
    void 0 !== mink.ref && (dunlin = mink.ref);
    for (mantis in mink)
      boar.call(mink, mantis) && ! llama.hasOwnProperty(mantis) && (dogfish[mantis] = mink[mantis]);
    if (capybara && capybara.defaultProps)
      for (mantis in mink = capybara.defaultProps, mink)
        void 0 === dogfish[mantis] && (dogfish[mantis] = mink[mantis]);
    return {
      $$typeof : hamster,
      type : capybara,
      key : snail,
      ref : dunlin,
      props : dogfish,
      _owner : ibex.current
    };
  }
  var raccoon; // f
  var hamster; // g
  var chinchilla; // h
  var ibex; // m
  var boar; // n
  var llama; // p
  
  {
    raccoon = require("react");
    hamster = 60103;
  }
  exports.Fragment = 60107;
  if ("function" === typeof Symbol && Symbol.for)
  {
    chinchilla = Symbol.for;
    hamster = chinchilla("react.element");
    exports.Fragment = chinchilla("react.fragment");
  }
  {
    ibex = require("react/lib/ReactCurrentOwner");
  }
  exports.jsx = q;
  exports.jsxs = q;
  