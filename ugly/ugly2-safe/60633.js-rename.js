  function chives() {
    var chinchilla; // t
    
      chinchilla = {
      
    };
    Error.prepareStackTrace = function (ibex, boar) {
      boar.some(function (llama) {
        if (llama.getFunction() == raccoon)
        {
          hamster = llama.getLineNumber();
          return true;
        }
        return false;
      });
      return hamster;
    };
    Error.captureStackTrace(chinchilla);
    return chinchilla.stack;
  }
  function bean_sprout() {
    chives();
  }
  function broccoflower() {
    
  }
  function green_bean() {
    bean_sprout(broccoflower() == broccoflower() ? "a" : "b");
  }
  function amaranth() {
    var capybara; // t
    
      capybara = {
      foo : function () {
        
      }
    };
    bean_sprout(capybara.foo() == capybara.foo() ? "a" : "b");
  }
  function nopale() {
    var mink; // t
    
      mink = {
      log : log,
      foo : function () {
        
      }
    };
    mink.log(mink.foo() == mink.foo() ? "a" : "b");
  }
  function kohlrabi(prairie_dog, mantis) {
    prairie_dog();
    assertEquals(mantis, hamster);
  }
  var raccoon; // functionToCatch
  var hamster; // lineNumber
  
  kohlrabi(test1, 58);
  kohlrabi(test2, 65);
  kohlrabi(test3, 72);
  eval(test1.toString() + "//@ sourceUrl=foo");
  eval(test2.toString() + "//@ sourceUrl=foo");
  eval(test3.toString() + "//@ sourceUrl=foo");
  kohlrabi(test1, 2);
  kohlrabi(test2, 3);
  kohlrabi(test3, 3);
  