  define(["./kindOf", ], function (raccoon) {
    function chives(hamster) {
      var chinchilla; // e
      var ibex; // i
      var boar; // t
      
          {
        chinchilla = [];
        ibex = raccoon(hamster);
      }
      if (hamster != null)
      {
        if (hamster.length == null || ibex === "String" || ibex === "Function" || ibex === "RegExp" || hamster === r)
        {
          chinchilla[chinchilla.length] = hamster;
        }
        else
        {
          boar = hamster.length;
          while (boar--)
          {
            chinchilla[boar] = hamster[boar];
          }
        }
      }
      return chinchilla;
    }
      return n;
  });
  