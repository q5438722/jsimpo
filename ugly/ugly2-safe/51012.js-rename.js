"use strict";
  function chives(snail, dunlin, termite) {
    return Math.min(Math.max(snail, dunlin), termite);
  }
  function bean_sprout(squid, rook) {
    return squid.indexOf(rook) > -1;
  }
  function broccoflower(woodcock, antelope) {
    return woodcock.apply(null, antelope);
  }
  function green_bean(goose) {
    var baboon; // t
    
      baboon = new RegExp("\\(([^)]+)\\)", "").exec(goose);
    return baboon ? baboon[1].split(",").map(function (bat) {
      return parseFloat(bat);
    }) : [];
  }
  function amaranth(bison, tarsier) {
    function pinto_bean(quelea) {
      var butterfly; // t
      
          butterfly = tarsier ? tarsier * quelea / 1000 : quelea;
      if (o < 1)
      {
        butterfly = Math.exp(- butterfly * o * woodpecker) * (f * Math.cos(c * butterfly) + l * Math.sin(c * butterfly));
      }
      else
      {
        butterfly = (f + l * butterfly) * Math.exp(- butterfly * woodpecker);
      }
      if (quelea === 0 || quelea === 1)
      {
        return quelea;
      }
      return 1 - butterfly;
    }
    function garlic() {
      var oyster; // e
      var stingray; // t
      var dragonfly; // r
      var finch; // n
      var meerkat; // a
      
          oyster = ibex.springs[bison];
      if (oyster)
      {
        return oyster;
      }
      stingray = 1 / 6;
      dragonfly = 0;
      while (true)
      {
        dragonfly += stingray;
        if (pinto_bean(dragonfly) === 1)
        {
          finch++;
          if (finch >= 16)
          {
            break;
          }
        }
        else
        {
          ;
        }
      }
      meerkat = dragonfly * stingray * 1000;
      ibex.springs[bison] = meerkat;
      return meerkat;
    }
      var cat; // e
    var weasel; // t
    var beaver; // n
    var komodo_dragon; // a
    var armadillo; // s
    var woodpecker; // u
    
      cat = green_bean(bison);
    weasel = chives(boar.und(cat[0]) ? 1 : cat[0], .1, 100);
    beaver = chives(boar.und(cat[1]) ? 100 : cat[1], .1, 100);
    komodo_dragon = chives(boar.und(cat[2]) ? 10 : cat[2], .1, 100);
    armadillo = chives(boar.und(cat[3]) ? 0 : cat[3], .1, 100);
    woodpecker = Math.sqrt(beaver / weasel);
    return tarsier ? v : g;
  }
  function nopale(alligator) {
    if (alligator === void 0)
      alligator = 10;
    return function (kookabura) {
      return Math.ceil(chives(kookabura, 0.000001, 1) * alligator) * (1 / alligator);
    };
  }
  function kohlrabi(human, reindeer) {
    var elk; // r
    var ape; // n
    var dinosaur; // a
    
      if (boar.fnc(human))
    {
      return human;
    }
    elk = human.split("(")[0];
    ape = capybara[elk];
    dinosaur = green_bean(human);
    switch (elk) {
      case "spring":
        return amaranth(human, reindeer);

      case "cubicBezier":
        return broccoflower(llama, dinosaur);

      case "steps":
        return broccoflower(steps, dinosaur);

      default:
        return broccoflower(ape, dinosaur);
      
    }
  }
  function tubers(kouprey) {
    var echidna; // t
    
      try {
      echidna = document.querySelectorAll(kouprey);
      return echidna;
    }
    catch (e) {
      return;
    }
  }
  function lima_bean(owl, gaur) {
    var locust; // r
    var magpie; // n
    var snake; // a
    var hummingbird; // i
    var sheep; // s
    
      locust = owl.length;
    magpie = arguments.length >= 2 ? arguments[1] : void 0;
    snake = [];
    {
      hummingbird = 0;
      for (; hummingbird < locust; hummingbird++)
      {
        if (hummingbird in owl)
        {
          sheep = owl[hummingbird];
          if (gaur.call(magpie, sheep, hummingbird, owl))
          {
            snake.push(sheep);
          }
        }
      }
    }
    return snake;
  }
  function white_radish(goldfinch) {
    return goldfinch.reduce(function (swan, oryx) {
      return swan.concat(boar.arr(oryx) ? white_radish(oryx) : oryx);
    }, 
    []);
  }
  function basil(turtle) {
    if (boar.arr(turtle))
    {
      return turtle;
    }
    if (boar.str(turtle))
    {
      turtle = tubers(turtle) || turtle;
    }
    if (turtle instanceof NodeList || turtle instanceof HTMLCollection)
    {
      return [].slice.call(turtle);
    }
    return [turtle, ];
  }
  function spaghetti_squash(cormorant, cattle) {
    return cormorant.some(function (tiger) {
      return tiger === cattle;
    });
  }
  function daikon(lion) {
    var frog; // t
    var skunk; // r
    
      frog = {
      
    };
    for (skunk in lion)
    {
      frog[skunk] = lion[skunk];
    }
    return frog;
  }
  function azuki_bean(lemur, goat) {
    var wolf; // r
    var gnat; // n
    
      wolf = daikon(lemur);
    for (gnat in lemur)
    {
      wolf[gnat] = goat.hasOwnProperty(gnat) ? goat[gnat] : lemur[gnat];
    }
    return wolf;
  }
  function shallot(quail, porpoise) {
    var chamois; // r
    var otter; // n
    
      chamois = daikon(quail);
    for (otter in porpoise)
    {
      chamois[otter] = boar.und(quail[otter]) ? porpoise[otter] : quail[otter];
    }
    return chamois;
  }
  function courgette(sand_dollar) {
    var jaguar; // t
    
      jaguar = new RegExp("rgb\\((\\d+,\\s*[\\d]+,\\s*[\\d]+)\\)", "g").exec(sand_dollar);
    return jaguar ? "rgba(" + jaguar[1] + ",1)" : sand_dollar;
  }
  function pumpkin(dugong) {
    var water_buffalo; // t
    var sea_urchin; // r
    var grouse; // n
    var sandpiper; // a
    var mule; // i
    var goldfish; // s
    
      water_buffalo = new RegExp("^#?([a-f\\d])([a-f\\d])([a-f\\d])$", "i");
    sea_urchin = dugong.replace(water_buffalo, function (caribou, raven, ram, salmon) {
      return raven + raven + ram + ram + salmon + salmon;
    });
    grouse = new RegExp("^#?([a-f\\d]{2})([a-f\\d]{2})([a-f\\d]{2})$", "i").exec(sea_urchin);
    sandpiper = parseInt(grouse[1], 16);
    mule = parseInt(grouse[2], 16);
    goldfish = parseInt(grouse[3], 16);
    return "rgba(" + sandpiper + "," + mule + "," + goldfish + ",1)";
  }
  function parsnip(cheetah) {
    function pea(nightingale, wombat, crane) {
      if (crane < 0)
      {
        crane = crane + 1;
      }
      if (crane > 1)
      {
        crane = crane - 1;
      }
      if (crane < 1 / 6)
      {
        return nightingale + (wombat - nightingale) * 6 * crane;
      }
      if (crane < 1 / 2)
      {
        return wombat;
      }
      if (crane < 2 / 3)
      {
        return nightingale + (wombat - nightingale) * (2 / 3 - crane) * 6;
      }
      return nightingale;
    }
      var viper; // t
    var pheasant; // r
    var pony; // n
    var hawk; // a
    var spoonbill; // i
    var swallow; // u
    var octopus; // o
    var newt; // c
    var caterpillar; // f
    var kingfisher; // l
    
      viper = new RegExp("hsl\\((\\d+),\\s*([\\d.]+)%,\\s*([\\d.]+)%\\)", "g").exec(cheetah) || new RegExp("hsla\\((\\d+),\\s*([\\d.]+)%,\\s*([\\d.]+)%,\\s*([\\d.]+)\\)", 
    "g").exec(cheetah);
    pheasant = parseInt(viper[1], 10) / 360;
    pony = parseInt(viper[2], 10) / 100;
    hawk = parseInt(viper[3], 10) / 100;
    spoonbill = viper[4] || 1;
    if (pony == 0)
    {
      swallow = octopus = newt = hawk;
    }
    else
    {
      caterpillar = hawk < .5 ? hawk * (1 + pony) : hawk + pony - hawk * pony;
      kingfisher = 2 * hawk - caterpillar;
      swallow = pea(kingfisher, caterpillar, pheasant + 1 / 3);
      octopus = pea(kingfisher, caterpillar, pheasant);
      newt = pea(kingfisher, caterpillar, pheasant - 1 / 3);
    }
    return "rgba(" + swallow * 255 + "," + octopus * 255 + "," + newt * 255 + "," + spoonbill + ")";
  }
  function navy_bean(chough) {
    if (boar.rgb(chough))
    {
      return courgette(chough);
    }
    if (boar.hex(chough))
    {
      return pumpkin(chough);
    }
    if (boar.hsl(chough))
    {
      return parsnip(chough);
    }
  }
  function skirret(goshawk) {
    var monkey; // t
    
      monkey = new RegExp("[+-]?\\d*\\.?\\d+(?:\\.\\d+)?(?:[eE][+-]?\\d+)?(%|px|pt|em|rem|in|cm|mm|ex|ch|pc|vw|vh|vmin|vmax|deg|rad|turn)?$", 
    "").exec(goshawk);
    if (monkey)
    {
      return monkey[1];
    }
  }
  function borlotti_bean(horse) {
    if (bean_sprout(horse, "translate") || horse === "perspective")
    {
      return "px";
    }
    if (bean_sprout(horse, "rotate") || bean_sprout(horse, "skew"))
    {
      return "deg";
    }
  }
  function anise(okapi, worm) {
    if (! boar.fnc(okapi))
    {
      return okapi;
    }
    return okapi(worm.target, worm.id, worm.total);
  }
  function mangetout(hornet, quetzal) {
    return hornet.getAttribute(quetzal);
  }
  function broad_beans(seahorse, chicken, mallard) {
    var aardvark; // n
    var albatross; // a
    var cobra; // s
    var walrus; // u
    var pelican; // o
    var mosquito; // c
    
      aardvark = skirret(chicken);
    if (spaghetti_squash([mallard, "deg", "rad", "turn", ], aardvark))
    {
      return chicken;
    }
    albatross = ibex.CSS[chicken + mallard];
    if (! boar.und(albatross))
    {
      return albatross;
    }
    cobra = document.createElement(seahorse.tagName);
    walrus = seahorse.parentNode && seahorse.parentNode !== document ? seahorse.parentNode : document.body;
    walrus.appendChild(cobra);
    cobra.style.position = "absolute";
    cobra.style.width = 100 + mallard;
    pelican = 100 / cobra.offsetWidth;
    walrus.removeChild(cobra);
    mosquito = pelican * parseFloat(chicken);
    ibex.CSS[chicken + mallard] = mosquito;
    return mosquito;
  }
  function topinambur(trout, whale, cassowary) {
    var guinea_fowl; // n
    var mouse; // a
    
      if (whale in trout.style)
    {
      guinea_fowl = whale.replace(new RegExp("([a-z])([A-Z])", "g"), "$1-$2").toLowerCase();
      mouse = trout.style[whale] || getComputedStyle(trout).getPropertyValue(guinea_fowl) || "0";
      return cassowary ? broad_beans(trout, mouse, cassowary) : mouse;
    }
  }
  function mushroom(wolverine, koala) {
    if (boar.dom(wolverine) && ! boar.inp(wolverine) && (! boar.nil(mangetout(wolverine, koala)) || boar.svg(wolverine) && wolverine[koala]))
    {
      return "attribute";
    }
    if (boar.dom(wolverine) && spaghetti_squash(chinchilla, koala))
    {
      return "transform";
    }
    if (boar.dom(wolverine) && (koala !== "transform" && topinambur(wolverine, koala)))
    {
      return "css";
    }
    if (wolverine[koala] != null)
    {
      return "object";
    }
  }
  function cayenne_pepper(giant_panda) {
    var mongoose; // t
    var mandrill; // r
    var moose; // n
    var sea_lion; // a
    
      if (! boar.dom(giant_panda))
    {
      return;
    }
    mongoose = giant_panda.style.transform || "";
    mandrill = new RegExp("(\\w+)\\(([^)]*)\\)", "g");
    moose = new Map;
    while (sea_lion = mandrill.exec(mongoose))
    {
      moose.set(sea_lion[1], sea_lion[2]);
    }
    return moose;
  }
  function bok_choy(leopard, gerbil, dog, vicuña) {
    var kangaroo; // a
    var shark; // i
    
      kangaroo = bean_sprout(gerbil, "scale") ? 1 : 0 + borlotti_bean(gerbil);
    shark = cayenne_pepper(leopard).get(gerbil) || kangaroo;
    if (dog)
    {
      dog.transforms.list.set(gerbil, shark);
      dog.transforms["last"] = gerbil;
    }
    return vicuña ? broad_beans(leopard, shark, vicuña) : shark;
  }
  function lentils(wallaby, crow, eagle, rail) {
    switch (mushroom(wallaby, crow)) {
      case "transform":
        return bok_choy(wallaby, crow, rail, eagle);

      case "css":
        return topinambur(wallaby, crow, eagle);

      case "attribute":
        return mangetout(wallaby, crow);

      default:
        return wallaby[crow] || 0;
      
    }
  }
  function artichoke(marten, jay) {
    var eland; // r
    var panther; // n
    var opossum; // a
    var shrew; // i
    
      eland = new RegExp("^(\\*=|\\+=|-=)", "").exec(marten);
    if (! eland)
    {
      return marten;
    }
    panther = skirret(marten) || 0;
    opossum = parseFloat(jay);
    shrew = parseFloat(marten.replace(eland[0], ""));
    switch (eland[0][0]) {
      case "+":
        return opossum + shrew + panther;

      case "-":
        return opossum - shrew + panther;

      case "*":
        return opossum * shrew + panther;

      
    }
  }
  function chamomile(duck, cod) {
    var crab; // r
    var bee; // n
    
      if (boar.col(duck))
    {
      return navy_bean(duck);
    }
    if (new RegExp("\\s", "g").test(duck))
    {
      return duck;
    }
    crab = skirret(duck);
    bee = crab ? duck.substr(0, duck.length - crab.length) : duck;
    if (cod)
    {
      return bee + cod;
    }
    return bee;
  }
  function fennel(wasp, stork) {
    return Math.sqrt(Math.pow(stork.x - wasp.x, 2) + Math.pow(stork.y - wasp.y, 2));
  }
  function calabrese(eel) {
    return Math.PI * 2 * mangetout(eel, "r");
  }
  function rhubarb(lapwing) {
    return mangetout(lapwing, "width") * 2 + mangetout(lapwing, "height") * 2;
  }
  function kidney_bean(guinea_pig) {
    return fennel({
      x : mangetout(guinea_pig, "x1"),
      y : mangetout(guinea_pig, "y1")
    }, 
    {
      x : mangetout(guinea_pig, "x2"),
      y : mangetout(guinea_pig, "y2")
    });
  }
  function jalapeno(ibis) {
    var camel; // t
    var lyrebird; // r
    var donkey; // n
    var clam; // a
    var grasshopper; // i
    
      camel = ibis.points;
    lyrebird = 0;
    {
      clam = 0;
      for (; clam < camel.numberOfItems; clam++)
      {
        grasshopper = camel.getItem(clam);
        if (clam > 0)
        {
          lyrebird += fennel(donkey, grasshopper);
        }
      }
    }
    return lyrebird;
  }
  function chard(gnu) {
    var mole; // t
    
      mole = gnu.points;
    return jalapeno(gnu) + fennel(mole.getItem(mole.numberOfItems - 1), mole.getItem(0));
  }
  function eggplant(penguin) {
    if (penguin.getTotalLength)
    {
      return penguin.getTotalLength();
    }
    switch (penguin.tagName.toLowerCase()) {
      case "circle":
        return calabrese(penguin);

      case "rect":
        return rhubarb(penguin);

      case "line":
        return kidney_bean(penguin);

      case "polyline":
        return jalapeno(penguin);

      case "polygon":
        return chard(penguin);

      
    }
  }
  function radish(elephant) {
    var dove; // t
    
      dove = eggplant(elephant);
    elephant.setAttribute("stroke-dasharray", dove);
    return dove;
  }
  function corn_salad(badger) {
    var guanaco; // t
    
      guanaco = badger.parentNode;
    while (boar.svg(guanaco))
    {
      if (! boar.svg(guanaco.parentNode))
      {
        break;
      }
      guanaco = guanaco.parentNode;
    }
    return guanaco;
  }
  function water_chestnut(pig, deer) {
    var gazelle; // r
    var giraffe; // n
    var rat; // a
    var starling; // i
    var emu; // s
    var bear; // u
    var kudu; // o
    
      gazelle = deer || {
      
    };
    giraffe = gazelle.el || corn_salad(pig);
    rat = giraffe.getBoundingClientRect();
    starling = mangetout(giraffe, "viewBox");
    emu = rat.width;
    bear = rat.height;
    kudu = gazelle.viewBox || (starling ? starling.split(" ") : [0, 0, emu, bear, ]);
    return {
      el : giraffe,
      viewBox : kudu,
      x : kudu[0] / 1,
      y : kudu[1] / 1,
      w : emu,
      h : bear,
      vW : kudu[2],
      vH : kudu[3]
    };
  }
  function lavender(fish, tapir) {
    var jellyfish; // r
    var loris; // n
    
      jellyfish = boar.str(fish) ? tubers(fish)[0] : fish;
    loris = tapir || 100;
    return function (coyote) {
      return {
        property : coyote,
        el : jellyfish,
        svg : water_chestnut(jellyfish),
        totalLength : eggplant(jellyfish) * (loris / 100)
      };
    };
  }
  function frisee(stinkbug, toad, chimpanzee) {
    function spinach(scorpion) {
      var squirrel; // t
      
          if (scorpion === void 0)
        scorpion = 0;
      squirrel = toad + scorpion >= 1 ? toad + scorpion : 0;
      return stinkbug.el.getPointAtLength(squirrel);
    }
      var louse; // a
    var turkey; // i
    var hyena; // s
    var sparrow; // u
    var hippopotamus; // o
    var cockroach; // c
    
      louse = water_chestnut(stinkbug.el, stinkbug.svg);
    turkey = spinach();
    hyena = spinach(-1);
    sparrow = spinach(+ 1);
    hippopotamus = chimpanzee ? 1 : louse.w / louse.vW;
    cockroach = chimpanzee ? 1 : louse.h / louse.vH;
    switch (stinkbug.property) {
      case "x":
        return (turkey.x - louse.x) * hippopotamus;

      case "y":
        return (turkey.y - louse.y) * cockroach;

      case "angle":
        return Math.atan2(sparrow.y - hyena.y, sparrow.x - hyena.x) * 180 / Math.PI;

      
    }
  }
  function potato(yak, gorilla) {
    var anteater; // r
    var ant; // n
    
      anteater = new RegExp("[+-]?\\d*\\.?\\d+(?:\\.\\d+)?(?:[eE][+-]?\\d+)?", "g");
    ant = chamomile(boar.pth(yak) ? yak.totalLength : yak, gorilla) + "";
    return {
      original : ant,
      numbers : ant.match(anteater) ? ant.match(anteater).map(Number) : [0, ],
      strings : boar.str(yak) || gorilla ? ant.split(anteater) : []
    };
  }
  function aubergine(spider) {
    var parrot; // t
    
      parrot = spider ? white_radish(boar.arr(spider) ? spider.map(toArray) : basil(spider)) : [];
    return lima_bean(parrot, function (fox, elephant_seal, dotterel) {
      return dotterel.indexOf(fox) === elephant_seal;
    });
  }
  function rosemary(rhinoceros) {
    var falcon; // r
    
      falcon = aubergine(rhinoceros);
    return falcon.map(function (pigeon, flamingo) {
      return {
        target : pigeon,
        id : flamingo,
        total : falcon.length,
        transforms : {
          list : cayenne_pepper(pigeon)
        }
      };
    });
  }
  function rutabaga(jackal, fly) {
    var vulture; // t
    var lobster; // r
    var buffalo; // a
    var peafowl; // i
    
      vulture = daikon(fly);
    if (new RegExp("^spring", "").test(vulture.easing))
    {
      vulture.duration = amaranth(vulture.easing);
    }
    if (boar.arr(jackal))
    {
      lobster = jackal.length;
      buffalo = lobster === 2 && ! boar.obj(jackal[0]);
      if (! buffalo)
      {
        if (! boar.fnc(fly.duration))
        {
          vulture.duration = fly.duration / lobster;
        }
      }
      else
      {
        jackal = {
          value : jackal
        };
      }
    }
    peafowl = boar.arr(jackal) ? jackal : [jackal, ];
    return peafowl.map(function (ferret, ox) {
      var polar_bear; // r
      
          polar_bear = boar.obj(ferret) && ! boar.pth(ferret) ? ferret : {
        value : ferret
      };
      if (boar.und(polar_bear.delay))
      {
        polar_bear.delay = ! ox ? fly.delay : 0;
      }
      if (boar.und(polar_bear.endDelay))
      {
        polar_bear.endDelay = ox === peafowl.length - 1 ? fly.endDelay : 0;
      }
      return polar_bear;
    }).map(function (narwhal) {
      return shallot(narwhal, vulture);
    });
  }
  function paprika(crocodile) {
    var curlew; // r
    var sardine; // a
    var herring; // e
    var red_panda; // n
    
      curlew = lima_bean(white_radish(crocodile.map(function (dolphin) {
      return Object.keys(dolphin);
    })), 
    function (salamander) {
      return boar.key(salamander);
    }).reduce(function (rabbit, zebra) {
      if (rabbit.indexOf(zebra) < 0)
      {
        rabbit.push(zebra);
      }
      return rabbit;
    }, 
    []);
    sardine = {
      
    };
    herring = function (gull) {
      var alpaca; // n
      
          alpaca = curlew[gull];
      sardine[alpaca] = crocodile.map(function (ostrich) {
        var manatee; // t
        var red_deer; // r
        
              manatee = {
          
        };
        for (red_deer in ostrich)
        {
          if (boar.key(red_deer))
          {
            if (red_deer == alpaca)
            {
              manatee.value = ostrich[red_deer];
            }
          }
          else
          {
            manatee[red_deer] = ostrich[red_deer];
          }
        }
        return manatee;
      });
    };
    {
      red_panda = 0;
      for (; red_panda < curlew.length; red_panda++)
        herring(red_panda);
    }
    return sardine;
  }
  function sage(barracuda, hare) {
    var hedgehog; // r
    var porcupine; // n
    var seal; // a
    
      hedgehog = [];
    porcupine = hare.keyframes;
    if (porcupine)
    {
      hare = shallot(paprika(porcupine), hare);
    }
    for (seal in hare)
    {
      if (boar.key(seal))
      {
        hedgehog.push({
          name : seal,
          tweens : rutabaga(hare[seal], barracuda)
        });
      }
    }
    return hedgehog;
  }
  function taro(lark, wren) {
    var partridge; // r
    var heron; // n
    var raccoon_1; // a
    
      partridge = {
      
    };
    for (heron in lark)
    {
      raccoon_1 = anise(lark[heron], wren);
      if (boar.arr(raccoon_1))
      {
        raccoon_1 = raccoon_1.map(function (hamster_1) {
          return anise(hamster_1, wren);
        });
        if (raccoon_1.length === 1)
        {
          raccoon_1 = raccoon_1[0];
        }
      }
      partridge[heron] = raccoon_1;
    }
    partridge.duration = parseFloat(partridge.duration);
    partridge.delay = parseFloat(partridge.delay);
    return partridge;
  }
  function legume(chinchilla_1, ibex_1) {
    var boar_1; // v
    
      return chinchilla_1.tweens.map(function (llama_1) {
      var capybara_1; // t
      var mink_1; // r
      var prairie_dog_1; // n
      var mantis_1; // a
      var dogfish_1; // i
      var snail_1; // s
      var dunlin_1; // u
      var termite_1; // o
      var squid_1; // c
      
          capybara_1 = taro(llama_1, ibex_1);
      mink_1 = capybara_1.value;
      prairie_dog_1 = boar.arr(mink_1) ? mink_1[1] : mink_1;
      mantis_1 = skirret(prairie_dog_1);
      dogfish_1 = lentils(ibex_1.target, chinchilla_1.name, mantis_1, ibex_1);
      snail_1 = boar_1 ? boar_1.to.original : dogfish_1;
      dunlin_1 = boar.arr(mink_1) ? mink_1[0] : snail_1;
      termite_1 = skirret(dunlin_1) || skirret(dogfish_1);
      squid_1 = mantis_1 || termite_1;
      if (boar.und(prairie_dog_1))
      {
        prairie_dog_1 = snail_1;
      }
      capybara_1.from = potato(dunlin_1, squid_1);
      capybara_1.to = potato(artichoke(prairie_dog_1, dunlin_1), squid_1);
      capybara_1.start = boar_1 ? boar_1.end : 0;
      capybara_1.end = capybara_1.start + capybara_1.delay + capybara_1.duration + capybara_1.endDelay;
      capybara_1.easing = kohlrabi(capybara_1.easing, capybara_1.duration);
      capybara_1.isPath = boar.pth(mink_1);
      capybara_1.isPathTargetInsideSVG = capybara_1.isPath && boar.svg(ibex_1.target);
      capybara_1.isColor = boar.col(capybara_1.from.original);
      if (capybara_1.isColor)
      {
        capybara_1.round = 1;
      }
      return capybara_1;
    });
  }
  function scallion(rook_1, woodcock_1) {
    var antelope_1; // t
    
      antelope_1 = rosemary(rook_1);
    antelope_1.forEach(function (goose_1) {
      var baboon_1; // t
      var bat_1; // r
      var bison_1; // n
      var tarsier_1; // a
      var cat_1; // i
      var weasel_1; // s
      var beaver_1; // u
      var komodo_dragon_1; // o
      
          for (baboon_1 in woodcock_1)
      {
        bat_1 = anise(woodcock_1[baboon_1], goose_1);
        bison_1 = goose_1.target;
        tarsier_1 = skirret(bat_1);
        cat_1 = lentils(bison_1, baboon_1, tarsier_1, goose_1);
        weasel_1 = tarsier_1 || skirret(cat_1);
        beaver_1 = artichoke(chamomile(bat_1, weasel_1), cat_1);
        komodo_dragon_1 = mushroom(bison_1, baboon_1);
        mink[komodo_dragon_1](bison_1, baboon_1, beaver_1, goose_1.transforms, true);
      }
    });
  }
  function gem_squash(armadillo_1, woodpecker_1) {
    var quelea_1; // r
    var butterfly_1; // n
    var oyster_1; // a
    
      quelea_1 = mushroom(armadillo_1.target, woodpecker_1.name);
    if (quelea_1)
    {
      butterfly_1 = legume(woodpecker_1, armadillo_1);
      oyster_1 = butterfly_1[butterfly_1.length - 1];
      return {
        type : quelea_1,
        property : woodpecker_1.name,
        animatable : armadillo_1,
        tweens : butterfly_1,
        duration : oyster_1.end,
        delay : butterfly_1[0].delay,
        endDelay : oyster_1.endDelay
      };
    }
  }
  function brussels_sprout(stingray_1, dragonfly_1) {
    return lima_bean(white_radish(stingray_1.map(function (finch_1) {
      return dragonfly_1.map(function (meerkat_1) {
        return gem_squash(finch_1, meerkat_1);
      });
    })), 
    function (alligator_1) {
      return ! boar.und(alligator_1);
    });
  }
  function carrot(kookabura_1, human_1) {
    var reindeer_1; // r
    var elk_1; // n
    var ape_1; // a
    
      reindeer_1 = kookabura_1.length;
    elk_1 = function (dinosaur_1) {
      return dinosaur_1.timelineOffset ? dinosaur_1.timelineOffset : 0;
    };
    ape_1 = {
      
    };
    ape_1.duration = reindeer_1 ? Math.max.apply(Math, kookabura_1.map(function (kouprey_1) {
      return elk_1(kouprey_1) + kouprey_1.duration;
    })) : human_1.duration;
    ape_1.delay = reindeer_1 ? Math.min.apply(Math, kookabura_1.map(function (echidna_1) {
      return elk_1(echidna_1) + echidna_1.delay;
    })) : human_1.delay;
    ape_1.endDelay = reindeer_1 ? ape_1.duration - Math.max.apply(Math, kookabura_1.map(function (owl_1) {
      return elk_1(owl_1) + owl_1.duration - owl_1.endDelay;
    })) : human_1.endDelay;
    return ape_1;
  }
  function marjoram(gaur_1) {
    var locust_1; // t
    var magpie_1; // r
    var snake_1; // n
    var hummingbird_1; // a
    var sheep_1; // i
    var goldfinch_1; // s
    var swan_1; // u
    
      locust_1 = azuki_bean(raccoon, gaur_1);
    magpie_1 = azuki_bean(hamster, gaur_1);
    snake_1 = sage(magpie_1, gaur_1);
    hummingbird_1 = rosemary(gaur_1.targets);
    sheep_1 = brussels_sprout(hummingbird_1, snake_1);
    goldfinch_1 = carrot(sheep_1, magpie_1);
    swan_1 = prairie_dog;
    prairie_dog++;
    return shallot(locust_1, {
      id : swan_1,
      children : [],
      animatables : hummingbird_1,
      animations : sheep_1,
      duration : goldfinch_1.duration,
      delay : goldfinch_1.delay,
      endDelay : goldfinch_1.endDelay
    });
  }
  function turnip() {
    return ! ! document && document.hidden;
  }
  function yam(oryx_1) {
    function watercress(lemur_1) {
      var goat_1; // t
      
          goat_1 = window.Promise && new Promise(function (wolf_1) {
        return ;
      });
      lemur_1.finished = goat_1;
      return goat_1;
    }
    function lemon_grass() {
      var gnat_1; // e
      
          gnat_1 = frog_1.direction;
      if (gnat_1 !== "alternate")
      {
        frog_1.direction = gnat_1 !== "normal" ? "normal" : "reverse";
      }
      frog_1.reversed = ! frog_1.reversed;
      tiger_1.forEach(function (quail_1) {
        return quail_1.reversed = frog_1.reversed;
      });
    }
    function radicchio(porpoise_1) {
      return frog_1.reversed ? frog_1.duration - porpoise_1 : porpoise_1;
    }
    function patty_pan() {
      turtle_1 = 0;
      cormorant_1 = radicchio(frog_1.currentTime) * (1 / anime.speed);
    }
    function herbs_and_spice(chamois_1, otter_1) {
      if (otter_1)
      {
        otter_1.seek(chamois_1 - otter_1.timelineOffset);
      }
    }
    function snap_pea(sand_dollar_1) {
      var jaguar_1; // t
      var dugong_1; // r
      
          if (! frog_1.reversePlayback)
      {
        jaguar_1 = 0;
        for (; jaguar_1 < lion_1; jaguar_1++)
        {
          herbs_and_spice(sand_dollar_1, tiger_1[jaguar_1]);
        }
      }
      else
      {
        dugong_1 = lion_1;
        for (; dugong_1--; )
        {
          herbs_and_spice(sand_dollar_1, tiger_1[dugong_1]);
        }
      }
    }
    function broccoli(water_buffalo_1) {
      var sea_urchin_1; // e
      var grouse_1; // r
      var sandpiper_1; // n
      var mule_1; // a
      var goldfish_1; // i
      var caribou_1; // s
      var raven_1; // u
      var ram_1; // o
      var salmon_1; // c
      var cheetah_1; // f
      var viper_1; // l
      var pheasant_1; // v
      var pony_1; // g
      var hawk_1; // m
      var spoonbill_1; // d
      var swallow_1; // p
      var octopus_1; // h
      var newt_1; // y
      var caterpillar_1; // b
      var kingfisher_1; // T
      var nightingale_1; // x
      var wombat_1; // M
      var crane_1; // A
      
          sea_urchin_1 = 0;
      grouse_1 = frog_1.animations;
      sandpiper_1 = grouse_1.length;
      while (sea_urchin_1 < sandpiper_1)
      {
        mule_1 = grouse_1[sea_urchin_1];
        goldfish_1 = mule_1.animatable;
        caribou_1 = mule_1.tweens;
        raven_1 = caribou_1.length - 1;
        ram_1 = caribou_1[raven_1];
        if (raven_1)
        {
          ram_1 = lima_bean(caribou_1, function (chough_1) {
            return water_buffalo_1 < chough_1.end;
          })[0] || ram_1;
        }
        salmon_1 = chives(water_buffalo_1 - ram_1.start - ram_1.delay, 0, ram_1.duration) / ram_1.duration;
        cheetah_1 = isNaN(salmon_1) ? 1 : ram_1.easing(salmon_1);
        viper_1 = ram_1.to.strings;
        pheasant_1 = ram_1.round;
        pony_1 = [];
        hawk_1 = ram_1.to.numbers.length;
        spoonbill_1 = void 0;
        {
          swallow_1 = 0;
          for (; swallow_1 < hawk_1; swallow_1++)
          {
            octopus_1 = void 0;
            newt_1 = ram_1.to.numbers[swallow_1];
            caterpillar_1 = ram_1.from.numbers[swallow_1] || 0;
            if (! ram_1.isPath)
            {
              octopus_1 = caterpillar_1 + cheetah_1 * (newt_1 - caterpillar_1);
            }
            else
            {
              octopus_1 = frisee(ram_1.value, cheetah_1 * newt_1, ram_1.isPathTargetInsideSVG);
            }
            if (pheasant_1)
            {
              if (! (ram_1.isColor && swallow_1 > 2))
              {
                octopus_1 = Math.round(octopus_1 * pheasant_1) / pheasant_1;
              }
            }
            pony_1.push(octopus_1);
          }
        }
        kingfisher_1 = viper_1.length;
        if (! kingfisher_1)
        {
          spoonbill_1 = pony_1[0];
        }
        else
        {
          spoonbill_1 = viper_1[0];
          {
            nightingale_1 = 0;
            for (; nightingale_1 < kingfisher_1; nightingale_1++)
            {
              wombat_1 = viper_1[nightingale_1 + 1];
              crane_1 = pony_1[nightingale_1];
              if (! isNaN(crane_1))
              {
                if (! wombat_1)
                {
                  spoonbill_1 += crane_1 + " ";
                }
                else
                {
                  spoonbill_1 += crane_1 + wombat_1;
                }
              }
            }
          }
        }
        mink[mule_1.type](goldfish_1.target, mule_1.property, spoonbill_1, goldfish_1.transforms);
        mule_1.currentValue = spoonbill_1;
        sea_urchin_1++;
      }
    }
    function celeriac(goshawk_1) {
      if (frog_1[goshawk_1] && ! frog_1.passThrough)
      {
        frog_1[goshawk_1](frog_1);
      }
    }
    function ginger() {
      if (frog_1.remaining && frog_1.remaining !== true)
      {
        frog_1.remaining--;
      }
    }
    function wasabi(monkey_1) {
      var horse_1; // t
      var okapi_1; // r
      var worm_1; // n
      var hornet_1; // a
      
          horse_1 = frog_1.duration;
      okapi_1 = frog_1.delay;
      worm_1 = horse_1 - frog_1.endDelay;
      hornet_1 = radicchio(monkey_1);
      frog_1.progress = chives(hornet_1 / horse_1 * 100, 0, 100);
      frog_1.reversePlayback = hornet_1 < frog_1.currentTime;
      if (tiger_1)
      {
        snap_pea(hornet_1);
      }
      if (! frog_1.began && frog_1.currentTime > 0)
      {
        frog_1.began = true;
        celeriac("begin");
      }
      if (! frog_1.loopBegan && frog_1.currentTime > 0)
      {
        frog_1.loopBegan = true;
        celeriac("loopBegin");
      }
      if (hornet_1 <= okapi_1 && frog_1.currentTime !== 0)
      {
        broccoli(0);
      }
      if (hornet_1 >= worm_1 && frog_1.currentTime !== horse_1 || ! horse_1)
      {
        broccoli(horse_1);
      }
      if (hornet_1 > okapi_1 && hornet_1 < worm_1)
      {
        if (! frog_1.changeBegan)
        {
          frog_1.changeBegan = true;
          frog_1.changeCompleted = false;
          celeriac("changeBegin");
        }
        celeriac("change");
        broccoli(hornet_1);
      }
      else
      {
        if (frog_1.changeBegan)
        {
          frog_1.changeCompleted = true;
          frog_1.changeBegan = false;
          celeriac("changeComplete");
        }
      }
      frog_1.currentTime = chives(hornet_1, 0, horse_1);
      if (frog_1.began)
      {
        celeriac("update");
      }
      if (monkey_1 >= horse_1)
      {
        cormorant_1 = 0;
        ginger();
        if (! frog_1.remaining)
        {
          frog_1.paused = true;
          if (! frog_1.completed)
          {
            frog_1.completed = true;
            celeriac("loopComplete");
            celeriac("complete");
            if (! frog_1.passThrough && "Promise" in window)
            {
              c();
              skunk_1 = watercress(frog_1);
            }
          }
        }
        else
        {
          turtle_1 = cattle_1;
          celeriac("loopComplete");
          frog_1.loopBegan = false;
          if (frog_1.direction === "alternate")
          {
            lemon_grass();
          }
        }
      }
    }
      var turtle_1; // i
    var cormorant_1; // s
    var cattle_1; // u
    var tiger_1; // o
    var lion_1; // n
    var frog_1; // P
    var skunk_1; // l
    
      if (oryx_1 === void 0)
      oryx_1 = {
        
      };
    {
      turtle_1 = 0;
      cormorant_1 = 0;
      cattle_1 = 0;
    }
    lion_1 = 0;
    frog_1 = marjoram(oryx_1);
    skunk_1 = watercress(frog_1);
    frog_1.reset = function () {
      var quetzal_1; // e
      var seahorse_1; // t
      
          quetzal_1 = frog_1.direction;
      frog_1.passThrough = false;
      frog_1.currentTime = 0;
      frog_1.progress = 0;
      frog_1.paused = true;
      frog_1.began = false;
      frog_1.loopBegan = false;
      frog_1.changeBegan = false;
      frog_1.completed = false;
      frog_1.changeCompleted = false;
      frog_1.reversePlayback = false;
      frog_1.reversed = quetzal_1 === "reverse";
      frog_1.remaining = frog_1.loop;
      tiger_1 = frog_1.children;
      lion_1 = tiger_1.length;
      {
        seahorse_1 = lion_1;
        for (; seahorse_1--; )
        {
          frog_1.children[seahorse_1].reset();
        }
      }
      if (frog_1.reversed && frog_1.loop !== true || quetzal_1 === "alternate" && frog_1.loop === 1)
      {
        frog_1.remaining++;
      }
      broccoli(frog_1.reversed ? frog_1.duration : 0);
    };
    frog_1._onDocumentVisibility = t;
    frog_1.set = function (chicken_1, mallard_1) {
      scallion(chicken_1, mallard_1);
      return frog_1;
    };
    frog_1.tick = function (aardvark_1) {
      cattle_1 = aardvark_1;
      if (! turtle_1)
      {
        turtle_1 = cattle_1;
      }
      wasabi((cattle_1 + (cormorant_1 - turtle_1)) * anime.speed);
    };
    frog_1.seek = function (albatross_1) {
      wasabi(radicchio(albatross_1));
    };
    frog_1.pause = function () {
      frog_1.paused = true;
      patty_pan();
    };
    frog_1.play = function () {
      if (! frog_1.paused)
      {
        return;
      }
      if (frog_1.completed)
      {
        frog_1.reset();
      }
      frog_1.paused = false;
      mantis.push(frog_1);
      patty_pan();
      dogfish();
    };
    frog_1.reverse = function () {
      lemon_grass();
      frog_1.completed = frog_1.reversed ? false : true;
      patty_pan();
    };
    frog_1.restart = function () {
      frog_1.reset();
      frog_1.play();
    };
    frog_1.remove = function (cobra_1) {
      var walrus_1; // t
      
          walrus_1 = aubergine(cobra_1);
      black_bean(walrus_1, frog_1);
    };
    frog_1.reset();
    if (frog_1.autoplay)
    {
      frog_1.play();
    }
    return frog_1;
  }
  function horseradish(pelican_1, mosquito_1) {
    var trout_1; // r
    
      {
      trout_1 = mosquito_1.length;
      for (; trout_1--; )
      {
        if (spaghetti_squash(pelican_1, mosquito_1[trout_1].animatable.target))
        {
          mosquito_1.splice(trout_1, 1);
        }
      }
    }
  }
  function black_bean(whale_1, cassowary_1) {
    var guinea_fowl_1; // r
    var mouse_1; // n
    var wolverine_1; // a
    var koala_1; // i
    var giant_panda_1; // s
    
      guinea_fowl_1 = cassowary_1.animations;
    mouse_1 = cassowary_1.children;
    horseradish(whale_1, guinea_fowl_1);
    {
      wolverine_1 = mouse_1.length;
      for (; wolverine_1--; )
      {
        koala_1 = mouse_1[wolverine_1];
        giant_panda_1 = koala_1.animations;
        horseradish(whale_1, giant_panda_1);
        if (! giant_panda_1.length && ! koala_1.children.length)
        {
          mouse_1.splice(wolverine_1, 1);
        }
      }
    }
    if (! guinea_fowl_1.length && ! mouse_1.length)
    {
      cassowary_1.pause();
    }
  }
  function chayote(mongoose_1) {
    var mandrill_1; // t
    var moose_1; // r
    var sea_lion_1; // n
    
      mandrill_1 = aubergine(mongoose_1);
    {
      moose_1 = mantis.length;
      for (; moose_1--; )
      {
        sea_lion_1 = mantis[moose_1];
        black_bean(mandrill_1, sea_lion_1);
      }
    }
  }
  function asparagus(leopard_1, gerbil_1) {
    var dog_1; // v
    var vicuña_1; // g
    var kangaroo_1; // m
    var shark_1; // d
    var wallaby_1; // p
    var crow_1; // h
    var eagle_1; // y
    var rail_1; // b
    var marten_1; // T
    var jay_1; // x
    var eland_1; // w
    var panther_1; // M
    var opossum_1; // A
    var shrew_1; // P
    var duck_1; // I
    
      if (gerbil_1 === void 0)
      gerbil_1 = {
        
      };
    dog_1 = gerbil_1.direction || "normal";
    vicuña_1 = gerbil_1.easing ? kohlrabi(gerbil_1.easing) : null;
    kangaroo_1 = gerbil_1.grid;
    shark_1 = gerbil_1.axis;
    wallaby_1 = gerbil_1.from || 0;
    crow_1 = wallaby_1 === "first";
    eagle_1 = wallaby_1 === "center";
    rail_1 = wallaby_1 === "last";
    marten_1 = boar.arr(leopard_1);
    jay_1 = marten_1 ? parseFloat(leopard_1[0]) : parseFloat(leopard_1);
    eland_1 = marten_1 ? parseFloat(leopard_1[1]) : 0;
    panther_1 = skirret(marten_1 ? leopard_1[1] : leopard_1) || 0;
    opossum_1 = gerbil_1.start || 0 + (marten_1 ? jay_1 : 0);
    shrew_1 = [];
    duck_1 = 0;
    return function (cod_1, crab_1, bee_1) {
      var wasp_1; // n
      var stork_1; // a
      var eel_1; // i
      var lapwing_1; // s
      var guinea_pig_1; // u
      var ibis_1; // o
      var camel_1; // c
      var lyrebird_1; // f
      var donkey_1; // l
      
          if (crow_1)
      {
        wallaby_1 = 0;
      }
      if (eagle_1)
      {
        wallaby_1 = (bee_1 - 1) / 2;
      }
      if (rail_1)
      {
        wallaby_1 = bee_1 - 1;
      }
      if (! shrew_1.length)
      {
        wasp_1 = 0;
        for (; wasp_1 < bee_1; wasp_1++)
        {
          if (! kangaroo_1)
          {
            shrew_1.push(Math.abs(wallaby_1 - wasp_1));
          }
          else
          {
            stork_1 = ! eagle_1 ? wallaby_1 % kangaroo_1[0] : (kangaroo_1[0] - 1) / 2;
            eel_1 = ! eagle_1 ? Math.floor(wallaby_1 / kangaroo_1[0]) : (kangaroo_1[1] - 1) / 2;
            lapwing_1 = wasp_1 % kangaroo_1[0];
            guinea_pig_1 = Math.floor(wasp_1 / kangaroo_1[0]);
            ibis_1 = stork_1 - lapwing_1;
            camel_1 = eel_1 - guinea_pig_1;
            lyrebird_1 = Math.sqrt(ibis_1 * ibis_1 + camel_1 * camel_1);
            if (shark_1 === "x")
            {
              lyrebird_1 = - ibis_1;
            }
            if (shark_1 === "y")
            {
              lyrebird_1 = - camel_1;
            }
            shrew_1.push(lyrebird_1);
          }
          duck_1 = Math.max.apply(Math, shrew_1);
        }
        if (vicuña_1)
        {
          shrew_1 = shrew_1.map(function (clam_1) {
            return vicuña_1(clam_1 / duck_1) * duck_1;
          });
        }
        if (dog_1 === "reverse")
        {
          shrew_1 = shrew_1.map(function (grasshopper_1) {
            return shark_1 ? grasshopper_1 < 0 ? grasshopper_1 * -1 : - grasshopper_1 : Math.abs(duck_1 - grasshopper_1);
          });
        }
      }
      donkey_1 = marten_1 ? (eland_1 - jay_1) / duck_1 : jay_1;
      return opossum_1 + donkey_1 * (Math.round(shrew_1[crab_1] * 100) / 100) + panther_1;
    };
  }
  function mung_bean(gnu_1) {
    var mole_1; // l
    
      if (gnu_1 === void 0)
      gnu_1 = {
        
      };
    mole_1 = yam(gnu_1);
    mole_1.duration = 0;
    mole_1.add = function (penguin_1, elephant_1) {
      function arugula(rat_1) {
        rat_1.passThrough = true;
      }
          var dove_1; // r
      var badger_1; // n
      var guanaco_1; // i
      var pig_1; // s
      var deer_1; // u
      var gazelle_1; // o
      var giraffe_1; // c
      
          dove_1 = mantis.indexOf(mole_1);
      badger_1 = mole_1.children;
      if (dove_1 > -1)
      {
        mantis.splice(dove_1, 1);
      }
      {
        guanaco_1 = 0;
        for (; guanaco_1 < badger_1.length; guanaco_1++)
        {
          arugula(badger_1[guanaco_1]);
        }
      }
      pig_1 = shallot(penguin_1, azuki_bean(hamster, gnu_1));
      pig_1.targets = pig_1.targets || gnu_1.targets;
      deer_1 = mole_1.duration;
      pig_1.autoplay = false;
      pig_1.direction = mole_1.direction;
      pig_1.timelineOffset = boar.und(elephant_1) ? deer_1 : artichoke(elephant_1, deer_1);
      arugula(mole_1);
      mole_1.seek(pig_1.timelineOffset);
      gazelle_1 = yam(pig_1);
      arugula(gazelle_1);
      badger_1.push(gazelle_1);
      giraffe_1 = carrot(badger_1, gnu_1);
      mole_1.delay = giraffe_1.delay;
      mole_1.endDelay = giraffe_1.endDelay;
      mole_1.duration = giraffe_1.duration;
      mole_1.seek(0);
      mole_1.reset();
      if (mole_1.autoplay)
      {
        mole_1.play();
      }
      return mole_1;
    };
    return mole_1;
  }
  var raccoon; // defaultInstanceSettings
  var hamster; // defaultTweenSettings
  var chinchilla; // validTransforms
  var ibex; // cache
  var boar; // is
  var llama; // bezier
  var capybara; // penner
  var mink; // setProgressValue
  var prairie_dog; // instanceID
  var mantis; // activeInstances
  var dogfish; // engine
  
  raccoon = {
    update : null,
    begin : null,
    loopBegin : null,
    changeBegin : null,
    change : null,
    changeComplete : null,
    loopComplete : null,
    complete : null,
    loop : 1,
    direction : "normal",
    autoplay : true,
    timelineOffset : 0
  };
  hamster = {
    duration : 1000,
    delay : 0,
    endDelay : 0,
    easing : "easeOutElastic(1, .5)",
    round : 0
  };
  boar = {
    arr : function (starling_1) {
      return Array.isArray(starling_1);
    },
    obj : function (emu_1) {
      return bean_sprout(Object.prototype.toString.call(emu_1), "Object");
    },
    pth : function (bear_1) {
      return boar.obj(bear_1) && bear_1.hasOwnProperty("totalLength");
    },
    svg : function (kudu_1) {
      return kudu_1 instanceof SVGElement;
    },
    inp : function (fish_1) {
      return fish_1 instanceof HTMLInputElement;
    },
    dom : function (tapir_1) {
      return tapir_1.nodeType || boar.svg(tapir_1);
    },
    str : function (jellyfish_1) {
      return typeof jellyfish_1 === "string";
    },
    fnc : function (loris_1) {
      return typeof loris_1 === "function";
    },
    und : function (coyote_1) {
      return typeof coyote_1 === "undefined";
    },
    nil : function (stinkbug_1) {
      return boar.und(stinkbug_1) || stinkbug_1 === null;
    },
    hex : function (toad_1) {
      return new RegExp("(^#[0-9A-F]{6}$)|(^#[0-9A-F]{3}$)", "i").test(toad_1);
    },
    rgb : function (chimpanzee_1) {
      return new RegExp("^rgb", "").test(chimpanzee_1);
    },
    hsl : function (louse_1) {
      return new RegExp("^hsl", "").test(louse_1);
    },
    col : function (turkey_1) {
      return boar.hex(turkey_1) || boar.rgb(turkey_1) || boar.hsl(turkey_1);
    },
    key : function (hyena_1) {
      return ! raccoon.hasOwnProperty(hyena_1) && ! hamster.hasOwnProperty(hyena_1) && hyena_1 !== "targets" && hyena_1 !== "keyframes";
    }
  };
  llama = function () {
    function thyme(sparrow_1, hippopotamus_1) {
      return 1 - 3 * hippopotamus_1 + 3 * sparrow_1;
    }
    function acorn_squash(cockroach_1, scorpion_1) {
      return 3 * scorpion_1 - 6 * cockroach_1;
    }
    function caraway(squirrel_1) {
      return 3 * squirrel_1;
    }
    function okra(yak_1, gorilla_1, anteater_1) {
      return ((thyme(gorilla_1, anteater_1) * yak_1 + acorn_squash(gorilla_1, anteater_1)) * yak_1 + caraway(gorilla_1)) * yak_1;
    }
    function cauliflower(ant_1, spider_1, parrot_1) {
      return 3 * thyme(spider_1, parrot_1) * ant_1 * ant_1 + 2 * acorn_squash(spider_1, parrot_1) * ant_1 + caraway(spider_1);
    }
    function kale(fox_1, elephant_seal_1, dotterel_1, rhinoceros_1, falcon_1) {
      var pigeon_1; // i
      var flamingo_1; // s
      var jackal_1; // u
      
          jackal_1 = 0;
      do
      {
        flamingo_1 = elephant_seal_1 + (dotterel_1 - elephant_seal_1) / 2;
        pigeon_1 = okra(flamingo_1, rhinoceros_1, falcon_1) - fox_1;
        if (pigeon_1 > 0)
        {
          ;
        }
        else
        {
          ;
        }
      }while (Math.abs(pigeon_1) > 1E-7 && ++ jackal_1 < 10);
      return flamingo_1;
    }
    function celery(fly_1, vulture_1, lobster_1, buffalo_1) {
      var peafowl_1; // a
      var narwhal_1; // i
      var ferret_1; // s
      
          {
        peafowl_1 = 0;
        for (; peafowl_1 < 4; ++ peafowl_1)
        {
          narwhal_1 = cauliflower(vulture_1, lobster_1, buffalo_1);
          if (narwhal_1 === 0)
          {
            return vulture_1;
          }
          ferret_1 = okra(vulture_1, lobster_1, buffalo_1) - fly_1;
          vulture_1 -= ferret_1 / narwhal_1;
        }
      }
      return vulture_1;
    }
    function sweet_potato(ox_1, polar_bear_1, crocodile_1, curlew_1) {
      function mamey(red_panda_1) {
        var rabbit_1; // t
        var zebra_1; // r
        var dolphin_1; // n
        var salamander_1; // a
        var gull_1; // i
        var alpaca_1; // s
        
              rabbit_1 = 0;
        zebra_1 = 1;
        dolphin_1 = f - 1;
        for (; zebra_1 !== dolphin_1 && sardine_1[zebra_1] <= red_panda_1; ++ zebra_1)
        {
          rabbit_1 += l;
        }
        -- zebra_1;
        salamander_1 = (red_panda_1 - sardine_1[zebra_1]) / (sardine_1[zebra_1 + 1] - sardine_1[zebra_1]);
        gull_1 = rabbit_1 + salamander_1 * l;
        alpaca_1 = cauliflower(gull_1, ox_1, crocodile_1);
        if (alpaca_1 >= .001)
        {
          return celery(red_panda_1, gull_1, ox_1, crocodile_1);
        }
        else
          if (alpaca_1 === 0)
          {
            return gull_1;
          }
          else
          {
            return kale(red_panda_1, rabbit_1, rabbit_1 + l, ox_1, crocodile_1);
          }
      }
          var sardine_1; // c
      var herring_1; // e
      
          if (! (0 <= ox_1 && ox_1 <= 1 && 0 <= crocodile_1 && crocodile_1 <= 1))
      {
        return;
      }
      sardine_1 = new Float32Array(f);
      if (ox_1 !== polar_bear_1 || crocodile_1 !== curlew_1)
      {
        herring_1 = 0;
        for (; herring_1 < f; ++ herring_1)
        {
          sardine_1[herring_1] = okra(herring_1 * l, ox_1, crocodile_1);
        }
      }
      return function (ostrich_1) {
        if (ox_1 === polar_bear_1 && crocodile_1 === curlew_1)
        {
          return ostrich_1;
        }
        if (ostrich_1 === 0 || ostrich_1 === 1)
        {
          return ostrich_1;
        }
        return okra(mamey(ostrich_1), polar_bear_1, curlew_1);
      };
    }
      return e;
  }();
  capybara = function () {
    var manatee_1; // t
    var red_deer_1; // r
    var barracuda_1; // e
    
      manatee_1 = {
      linear : function () {
        return function (hare_1) {
          return hare_1;
        };
      }
    };
    red_deer_1 = {
      Sine : function () {
        return function (hedgehog_1) {
          return 1 - Math.cos(hedgehog_1 * Math.PI / 2);
        };
      },
      Circ : function () {
        return function (porcupine_1) {
          return 1 - Math.sqrt(1 - porcupine_1 * porcupine_1);
        };
      },
      Back : function () {
        return function (seal_1) {
          return seal_1 * seal_1 * (3 * seal_1 - 2);
        };
      },
      Bounce : function () {
        return function (lark_1) {
          var wren_1; // t
          var partridge_1; // r
          
                  partridge_1 = 4;
          while (lark_1 < ((wren_1 = Math.pow(2, -- partridge_1)) - 1) / 11)
          {
            
          }
          return 1 / Math.pow(4, 3 - partridge_1) - 7.5625 * Math.pow((wren_1 * 3 - 2) / 22 - lark_1, 2);
        };
      },
      Elastic : function (heron_1, raccoon_2) {
        var hamster_2; // r
        var chinchilla_2; // n
        
              if (heron_1 === void 0)
          heron_1 = 1;
        if (raccoon_2 === void 0)
          raccoon_2 = .5;
        hamster_2 = chives(heron_1, 1, 10);
        chinchilla_2 = chives(raccoon_2, .1, 2);
        return function (ibex_2) {
          return ibex_2 === 0 || ibex_2 === 1 ? ibex_2 : - hamster_2 * Math.pow(2, 10 * (ibex_2 - 1)) * Math.sin((ibex_2 - 1 - chinchilla_2 / (Math.PI * 2) * Math.asin(1 / hamster_2)) * (Math.PI * 2) / chinchilla_2);
        };
      }
    };
    barracuda_1 = ["Quad", "Cubic", "Quart", "Quint", "Expo", ];
    barracuda_1.forEach(function (boar_2, llama_2) {
      red_deer_1[boar_2] = function () {
        return function (capybara_2) {
          return Math.pow(capybara_2, llama_2 + 2);
        };
      };
    });
    Object.keys(red_deer_1).forEach(function (mink_2) {
      var prairie_dog_2; // n
      
          prairie_dog_2 = red_deer_1[mink_2];
      manatee_1["easeIn" + mink_2] = prairie_dog_2;
      manatee_1["easeOut" + mink_2] = function (mantis_2, dogfish_2) {
        return function (snail_2) {
          return 1 - prairie_dog_2(mantis_2, dogfish_2)(1 - snail_2);
        };
      };
      manatee_1["easeInOut" + mink_2] = function (dunlin_2, termite_2) {
        return function (squid_2) {
          return squid_2 < .5 ? prairie_dog_2(dunlin_2, termite_2)(squid_2 * 2) / 2 : 1 - prairie_dog_2(dunlin_2, termite_2)(squid_2 * -2 + 2) / 2;
        };
      };
      manatee_1["easeOutIn" + mink_2] = function (rook_2, woodcock_2) {
        return function (antelope_2) {
          return antelope_2 < .5 ? (1 - prairie_dog_2(rook_2, woodcock_2)(1 - antelope_2 * 2)) / 2 : (prairie_dog_2(rook_2, woodcock_2)(antelope_2 * 2 - 1) + 1) / 2;
        };
      };
    });
    return manatee_1;
  }();
  mantis = [];
  dogfish = function () {
    function new_zealand_spinach() {
      if (! goose_2 && (! turnip() || ! anime.suspendWhenDocumentHidden) && mantis.length > 0)
      {
        goose_2 = requestAnimationFrame(i);
      }
    }
    function dill(baboon_2) {
      var bat_2; // t
      var bison_2; // r
      var tarsier_2; // n
      
          bat_2 = mantis.length;
      bison_2 = 0;
      while (bison_2 < bat_2)
      {
        tarsier_2 = mantis[bison_2];
        if (! tarsier_2.paused)
        {
          tarsier_2.tick(baboon_2);
          bison_2++;
        }
        else
        {
          mantis.splice(bison_2, 1);
          bat_2--;
        }
      }
    }
    function lettuce() {
      if (! anime.suspendWhenDocumentHidden)
      {
        return;
      }
      if (turnip())
      {
        goose_2 = cancelAnimationFrame(goose_2);
      }
      else
      {
        mantis.forEach(function (cat_2) {
          return cat_2._onDocumentVisibility();
        });
        dogfish();
      }
    }
      var goose_2; // a
    
      if (typeof document !== "undefined")
    {
      document.addEventListener("visibilitychange", t);
    }
    return e;
  }();
  anime.version = "3.2.1";
  anime.speed = 1;
  anime.suspendWhenDocumentHidden = true;
  anime.running = mantis;
  anime.remove = removeTargetsFromActiveInstances;
  anime.get = getOriginalTargetValue;
  anime.set = setTargetsValue;
  anime.convertPx = convertPxToUnit;
  anime.path = getPath;
  anime.setDashoffset = setDashoffset;
  anime.stagger = stagger;
  anime.timeline = timeline;
  anime.easing = parseEasings;
  anime.penner = capybara;
  anime.random = function (weasel_2, beaver_2) {
    return Math.floor(Math.random() * (beaver_2 - weasel_2 + 1)) + weasel_2;
  };
  module.exports = anime;
  