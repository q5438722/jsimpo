var { addComment } = require("./add-comment");

var { addLabels } = require("./add-labels");

var { closeOpen } = require("./close-open");

var { labeler } = require("./labeler");

module.exports = { addComment: addComment, addLabels: addLabels, closeOpen: closeOpen, labeler: labeler };
