let assert,pullAll;_982‍.w("assert",[["default",["assert"],function(v){assert=v}]]);_982‍.w("../pullAll.js",[["default",["pullAll"],function(v){pullAll=v}]]);


describe('pullAll', function() {
  it('should work with the same value for `array` and `values`', function() {
    var array = [{ 'a': 1 }, { 'b': 2 }],
        actual = pullAll(array, array);

    assert.deepStrictEqual(actual, []);
  });
});
