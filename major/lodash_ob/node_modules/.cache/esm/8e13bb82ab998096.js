let assert,lodashStable,MAX_SAFE_INTEGER,stubTrue,stubFalse,_isIndex;_ab9‍.w("assert",[["default",["assert"],function(v){assert=v}]]);_ab9‍.w("lodash",[["default",["lodashStable"],function(v){lodashStable=v}]]);_ab9‍.w("./utils.js",[["MAX_SAFE_INTEGER",["MAX_SAFE_INTEGER"],function(v){MAX_SAFE_INTEGER=v}],["stubTrue",["stubTrue"],function(v){stubTrue=v}],["stubFalse",["stubFalse"],function(v){stubFalse=v}]]);_ab9‍.w("../.internal/isIndex.js",[["default",["_isIndex"],function(v){_isIndex=v}]]);




describe('isIndex', function() {
  var func = _isIndex;

  it('should return `true` for indexes', function() {
    if (func) {
      var values = [[0], ['0'], ['1'], [3, 4], [MAX_SAFE_INTEGER - 1]],
          expected = lodashStable.map(values, stubTrue);

      var actual = lodashStable.map(values, function(args) {
        return func.apply(undefined, args);
      });

      assert.deepStrictEqual(actual, expected);
    }
  });

  it('should return `false` for non-indexes', function() {
    if (func) {
      var values = [['1abc'], ['07'], ['0001'], [-1], [3, 3], [1.1], [MAX_SAFE_INTEGER]],
          expected = lodashStable.map(values, stubFalse);

      var actual = lodashStable.map(values, function(args) {
        return func.apply(undefined, args);
      });

      assert.deepStrictEqual(actual, expected);
    }
  });
});
