import{bidi}from"../../src/core/bidi.js";describe("bidi",function(){it("should mark text as RTL if more than 30% of text is RTL",function(){const t="مصر Egypt";const i="Egypt رصم";const s=bidi(t,-1,false);expect(s.str).toEqual(i);expect(s.dir).toEqual("rtl")});it("should mark text as LTR if less than 30% of text is RTL",function(){const t="Egypt is known as مصر in Arabic.";const i="Egypt is known as رصم in Arabic.";const s=bidi(t,-1,false);expect(s.str).toEqual(i);expect(s.dir).toEqual("ltr")})});