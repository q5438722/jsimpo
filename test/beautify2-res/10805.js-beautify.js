import{dispatch}from'../../app';import{SHOW_NOTIFICATION,HIDE_NOTIFICATION}from'./constants';let nextNotificationId=0;export function showNotification(message,status){nextNotificationId++;(id=>{setTimeout(()=>{dispatch(hideNotification(id))},2500)})(nextNotificationId);return{type:SHOW_NOTIFICATION,message,status,id:nextNotificationId,}}export function hideNotification(id){return{type:HIDE_NOTIFICATION,id,}}
